# New Base Manifest

## Strategy ID

`SUPPLY_DEMAND_DEMAND_ONLY_EDGE_V1`

## Promotion Note

The prior active base `SUPPLY_DEMAND_CONCEPT_EDGE_V1` traded both demand and supply zones. Direction OOS research showed that demand-only improved every headline metric while supply-only degraded out of sample:

- Both-side B: `372` trades, `$1,688.05`, `68.01%` WR, `PF 1.51`, `0.0908R`
- Demand-only: `211` trades, `$2,186.91`, `69.19%` WR, `PF 2.38`, `0.2073R`
- Supply-only: `161` trades, `$-498.87`, `66.46%` WR, `PF 0.71`, `-0.0620R`

Train/test proof:

- Train `2019-2022`: demand PF `1.88`, supply PF `1.18`
- Test `2023-2026`: demand PF `2.92`, supply PF `0.50`
- Post-2025: demand PF `2.34`, supply PF `0.43`

Therefore the active forward-looking baseline is demand-only. The prior both-side base remains historical reference, not the active strategy.

## Promoted Corrections

The old frozen audit baseline remains archived. This new base promotes only the corrections that survived the research pass as structural improvements:

1. Partial take profit at original swing target.
2. Runner on the remaining half.
3. Runner stop moves to breakeven after T1.
4. Runner exits at a 24-hour wall-clock time stop if still open.
5. Zone expiry is `48h` wall-clock.
6. Execution is M15 close trigger + close fill.
7. Active reporting starts at `2019-06-03 15:00:00+00:00`, excluding the pre-2019 daily-only source segment.
8. `swing_right < 1` is rejected as a safety guard; the active strategy value remains `swing_right = 2`.
9. Direction is restricted to `demand` zones only after the original both-side no-overlap arbitration. This preserves the exact 211-trade OOS-validated result rather than creating a new 216-trade long-only variant.

## Not Promoted Into Base

These remain research-only and are not part of the new base:

- Hour blacklist
- Day-of-week sizing
- Retest-depth filter
- Impulse cap
- Combined filters

Reason: they improved selected in-sample slices but did not have enough robustness to become the active base.

## Latest Verified Metrics

Generated by:

```bash
PYTHONPATH=src python3 scripts/run_supply_demand_concept.py
```

Metrics at `$50` fixed risk:

- Data scope: from `2019-06-03 15:00:00+00:00`
- Trades: `211`
- Net PnL: `$2,186.91`
- Win rate: `69.19%`
- Profit factor: `2.38`
- Expectancy: `0.2073R/trade`
- Average win: `0.517R`
- Average loss: `0.489R`

## Test Gate

Generated by:

```bash
python3 -m pytest tests
```

Latest result: `11 passed`.
