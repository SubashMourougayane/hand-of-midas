# Session Handoff — 2026-06-15

## Theme
Full `fe-remodel` frontend redesign + Gold Micro market_close ship + DWX EA fix + side-by-side staging deploy on VPS.

## What Happened (Chronological)

**Phase 1 — fe-remodel branch (18 commits, ~7k LOC).** Started a new `fe-remodel` branch off `midas-deploy` and built the full Old-Money trading-desk redesign end-to-end:

- Tokens + next/font (Geist Sans + Mono + Instrument Serif italic display)
- 13 component primitives (Button, Card, Stat, Badge, Tabs, Sheet, Skeleton, Table, Sparkline, Tooltip, StatusDot, EmptyState, PageHeader)
- 4 motion primitives (CountUp, Reveal, MarqueeTicker, Display) + animation library (hom-stagger, hom-lift, hom-glow)
- App shell (TopBar, SystemSwitcher with ⌘1-4 hotkeys, SectionNav with brass rail)
- `lib/client.ts` consolidated MidasClient + 6 SWR hooks
- Page rewrites: /trades, /journal, /backtest, /architecture, /live, /settings, /login, /
- Hardening: `/live` 979 LOC → 381 (subcomponents extracted to `components/live/`), lint 19 errors → 0, TradeJourney + PnlCalendar + landing token-swept
- Step 9: dropped Next.js /api proxy, hits hosted API directly via `NEXT_PUBLIC_API_BASE`
- Step 10: journal staleness UX (banner + per-row age pill when newest event > 2h old)

User chose 24h staging-soak before merging — branch is **NOT yet merged** to midas-deploy.

**Phase 2 — Gold Micro market_close filter SHIPPED.** Per-system kwarg-gated disable of the 21-22 UTC market-close block (inherited from Oil Micro config). BT 21yr: PF 4.40→4.52, P&L +$26,852 (+8.0%), N+42. Oil Micro untouched. Regression check confirmed bit-identical for the 3 untouched systems.

**Phase 3 — DWX EA FILE_SHARE bug.** User reported 5004 (`ERR_FILE_CANNOT_OPEN`) bursts in MT5 Experts log. Investigation via debug API (terminal log + DWX folder mtimes) showed the bursts cluster on broker reconnect cycles. Root cause: `WriteFile`/`ReadFile` opened files with exclusive locks (no `FILE_SHARE_*` flags). Concurrent reader (Python backends, MT5 internal sync) blocked EA writes. Fix: add `FILE_SHARE_READ|FILE_SHARE_WRITE` to both functions. EA recompiled + re-attached on VPS chart.

**Phase 4 — Backend CORS expansion.** All 4 backends had `allow_origins=["http://localhost:3000", "http://localhost:3001"]`. Added localhost:3002, https://midas.subashtrades.in (was missing), https://staging.midas.subashtrades.in. All 4 services restarted on VPS.

**Phase 5 — Side-by-side staging deploy on VPS.** Stood up `staging.midas.subashtrades.in` alongside prod:
- Cloned repo to `C:\hand-of-midas-staging\` (separate from prod checkout)
- `npm install` + `npm run build` for fe-remodel branch
- Started `next start -p 3002` (NOT yet registered as NSSM service)
- DNS A record at GoDaddy: `staging.midas` → 84.247.177.145
- Caddy second virtual host block, auto-issued TLS cert
- Discovered + fixed Caddy directive ordering bug: `/api/oil-micro/*` MUST precede `/api/oil/*` (path matching is order-dependent). PROD block was also broken (oil-micro line missing entirely).

End state: prod at https://midas.subashtrades.in/ + staging at https://staging.midas.subashtrades.in/ both serving cleanly. All 4 backends listening, all health probes 200.

## What's Live

- **midas-deploy HEAD:** `228be1b` (pushed)
- **fe-remodel HEAD:** `83014c5` (pushed, NOT merged)
- **VPS prod:** https://midas.subashtrades.in/ (port 3001, midas-deploy frontend)
- **VPS staging:** https://staging.midas.subashtrades.in/ (port 3002, fe-remodel frontend, same backend)
- **Backend services:** all 4 restarted with new CORS list, all responding 200
- **EA:** DWX_Server.mq5 recompiled with FILE_SHARE fix, attached to XAU/USD chart
- **Caddyfile:** prod + staging blocks, oil-micro routing now correct in both

## Commits This Session

### `midas-deploy` (4 commits ahead of `4743437`)
| SHA | Description |
|---|---|
| `63c75f1` | Ship: Gold Micro market_close 21-22 UTC drop |
| `ae7f9c4` | Merge filter: Gold Micro market_close drop (+$26.8k/21yr) |
| `98cd558` | DWX EA: add FILE_SHARE flags to WriteFile/ReadFile |
| `228be1b` | Backend: expand CORS allow-list to include staging + prod origins |

### `fe-remodel` (18 commits ahead of `4743437`)
| SHA | Description |
|---|---|
| `5614ac3` | step 1: design tokens + next/font |
| `4776620` | step 2: components/ui/ primitives + /uikit gallery |
| `d1e3096` | step 3: app shell + system switcher |
| `f611250` | step 3.5: old-money palette + serif headlines + animations |
| `390885c` | step 4: lib/client.ts + SWR hooks |
| `7323b7e` | step 5a: /trades rewrite |
| `b1cf82f` | step 5b: /journal rewrite |
| `d578778` | step 5c: /backtest rewrite |
| `208c42e` | step 5d: /architecture rewrite |
| `b45c60b` | step 5e: /live rewrite |
| `a2c0053` | step 5f: /settings + /login + / restyle |
| `7ef5710` | step 6: cleanup |
| `3177670` | step 7: full API integration |
| `3903e1d` | step 8a: extract /live subcomponents |
| `f27aeaf` | step 8b: lint pass — 19 errors → 0 |
| `1acd65c` | step 8c: font + token sweep on lagging components |
| `f506c11` | step 9: hit hosted API directly + fix backtest stale-result bug |
| `83014c5` | step 10: journal staleness UX |

## Outstanding Issues

### MEDIUM
- **fe-remodel NOT merged to midas-deploy.** User chose 24h staging-soak. Real-data SSE smoke test pending Sun 21:00 UTC market reopen.
- **Staging frontend not registered as NSSM service.** Currently a foreground `npm run start` on port 3002 — dies on VPS reboot. NSSM register script in [[project_staging_environment]] memory.
- **EA symlink not restored on VPS.** EA is currently a copy at `Common\Files\...\Experts\DWX_Server.mq5`. Future EA updates require re-copy after `git pull` until admin shell can run `mklink`.

### LOW
- **3 untested filters remain:** #4 Anti-Trend-Extension, #9 R:R Upper Bound, #12 Engulfing-of-doji. All same shape as prior 9 disproven hypotheses; likely all stash.
- **First Filter #7 partial-TP postmortem** still pending (waiting on first qualifying live trade).
- **`logs/*.log` should be in `.gitignore`.** They're noise; the services rewrite them every minute. Tracking causes "modified but can't unlink" friction on every git pull on VPS.

## Decisions Made

1. **fe-remodel staging-first, merge later.** User chose 24h staging-soak rather than direct merge. Risk profile: low (frontend-only, backend untouched), but want to see real SSE + first trade fire before committing. Locked in [[session_2026_06_15]].
2. **Gold Micro market_close ship is per-system, not global.** Oil Micro keeps the gate (its block is correct). Configured via `MICRO_ALPHA_SWEEP['disable_market_close']` flag in `backend-micro/config.py` only.
3. **CORS option chosen over env-var rebuild.** Adding staging origin to backend allow_origins was the simpler path vs. rebuilding fe-remodel staging with `NEXT_PUBLIC_API_BASE=https://staging...`. Three-line change × 4 services + restart.
4. **Caddy directive ordering is fragile.** `/api/oil-micro/*` MUST precede `/api/oil/*`. PROD block was missing oil-micro entirely; fixed during staging setup. Documented in [[project_staging_environment]].
5. **No frontend bundle env-var rebuild for staging.** Staging frontend uses default `https://midas.subashtrades.in` API_BASE (cross-origin to its own domain). Backend CORS expansion makes it work without rebuild.

## What's Next (resume order)

1. **Sun 21:00 UTC market reopen.** Watch staging at https://staging.midas.subashtrades.in/live during a real scan/entry/exit cycle. If everything renders cleanly, merge `fe-remodel` → `midas-deploy` (squash merge) and deploy.
2. **Register `staging-frontend` as NSSM service** (script in [[project_staging_environment]]). Currently a foreground process that dies on reboot.
3. **First Filter #7 partial-TP postmortem** when the first qualifying Alpha-Sweep trade fires.
4. **Run remaining 3 untested filters** (#4, #9, #12) for completeness — all expected to stash per the 9-filter pattern.
5. **Add `logs/*.log` to `.gitignore`** to stop the constant "modified but can't unlink" friction on VPS pulls.

## File Inventory

### `midas-deploy` modified
- `backend-micro/backtest/engine.py`, `backend-micro/scanner/scheduler.py`, `backend-micro/config.py`, `backend/strategies/micro_alpha_sweep.py` — Gold Micro market_close kwarg-gating + flag flip
- `mql5/DWX_Server.mq5` — FILE_SHARE flags
- `backend/main.py`, `backend-oil/main.py`, `backend-micro/main.py`, `backend-oil-micro/main.py` — CORS allow_origins expansion
- `scripts/run_filter_gold_micro_market_close.py` — measurement runner

### `fe-remodel` new files
- `frontend/components/ui/{Button,Card,Stat,Badge,Tabs,Sheet,Skeleton,Table,Sparkline,Tooltip,StatusDot,EmptyState,PageHeader,Display,CountUp,Reveal,MarqueeTicker,cn,index}.tsx` — primitives
- `frontend/components/shell/{AppShell,TopBar,SystemSwitcher,SectionNav,UserMenu}.tsx` — app shell
- `frontend/components/live/{SystemMode,MicroWindows,SweepProximity}.tsx` — extracted from /live monolith
- `frontend/lib/client.ts`, `frontend/lib/hooks/{useSystemParam,useTrades,useJournal,useScanStatus,useLatestBacktest,useLiveStream,index}.ts` — data layer
- `frontend/app/uikit/page.tsx` — design language gallery (hidden, not linked from nav)

### `fe-remodel` modified (key files)
- `frontend/app/{globals.css,layout.tsx,page.tsx,login/page.tsx,trades/page.tsx,journal/page.tsx,backtest/page.tsx,architecture/page.tsx,live/page.tsx,settings/page.tsx}` — every page rewritten/restyled
- `frontend/components/{InstrumentProvider,TradeJourney,PnlCalendar,DatePicker}.tsx` — token-swept
- `frontend/contexts/AuthContext.tsx` — uses API_BASE from client.ts
- `frontend/lib/api.ts` — gutted to types-only
- `frontend/eslint.config.mjs` — React 19 stylistic rules downgraded to warn
- `frontend/next.config.ts` — /api rewrites removed, allowedDevOrigins added
- `frontend/package.json` — added geist, @fontsource/instrument-serif, swr

### `fe-remodel` deleted
- `frontend/components/Sidebar.tsx` (replaced by app shell)

## Numbers to Remember

| Metric | Value |
|---|---|
| Cumulative shipped P&L (filter sweep) | **+$1,547,579 / 21yr (+$73.7k/yr)** |
| Gold Micro market_close ship impact | +$26,852 / 21yr (+8.0%, +0.13 PF) |
| fe-remodel commits | 18 |
| fe-remodel LOC delta | ~+7k / -3k across 30+ files |
| Build verified | 10/10 routes, 0 type errors, 0 lint errors |
| `/live` LOC | 979 → 381 (subcomponents extracted) |
| DWX EA error 5004 burst | 75 errors @ 2026-06-13 19:01 UTC (last; pre-fix) |
| Schema migration runtime via debug API | 929ms |
| Filter sweep state | 5 shipped / 12 rejected / 1 reverted / 3 untested |

## What I'd Do If I Had One More Hour

Register the `staging-frontend` NSSM service so VPS reboots don't kill it. Currently a foreground node process that survives only as long as the parent shell context. Three commands from elevated PowerShell — written up in `[[project_staging_environment]]` memory. Without it, any VPS reboot (Windows Update, etc.) silently takes staging offline.

Alternatively: run the parity harness once against staging to sanity-check that the new EventSource path on /live doesn't drop SSE events under proxy load. Would catch any subtle reconnect bugs before Sunday's market open.
