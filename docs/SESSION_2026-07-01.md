# Session Handoff — 2026-07-01

## Theme

Ported A+D intraday Fib V2 to bt_engine end-to-end, executed full audit + parity verification, built Model B equity sizer + live runner wiring + DWX EA M15 patch, launched first paper-live run on JustMarkets-Demo2.

## What Happened (Chronological)

### Part A — Dashboard refinement (continuation from prior session)
- Mar-Apr-May 2026 dashboard already shipped previous session
- Added monthly_breakdown.csv + REPORT.md polishing
- Added hold-time column (M5 bar count → human-readable)
- Added $ PnL columns (1.5% risk on $5k account)
- Identified strategy is NOT intraday — 5-6 day max hold via 72h cap

### Part B — Intraday research
- Ran 12h-cap pilot on M15 base → A+D survives strongly
- Generated 10,800-config M15 sweep → 5 top survivors picked
- Ran 5-survivor × 6-test adversarial battery → all 5 PASS
- User picked A+D combo: A (long, lb=3, hold=12h, london_ny, PTP+1R), D (short, lb=3, hold=24h, all, PTP+1R)

### Part C — Risk model selection
- Tested 0.5% / 1.0% / 1.5% / 2.0% sizing
- Modeled fixed-risk vs yearly-compound vs Model B asymmetric monthly
- User locked **Model B 1.5% asymmetric monthly** as production target

### Part D — bt_engine port
Phase 0-8 strict gate flow:
- Phase 0: research dedup parity baseline parquets
- Phase 1-2: state field + intraday module
- Phase 3-4: M15 pivot + dedup + min_risk unit tests
- Phase 5: 20k-bar integration smoke
- Phase 6: BLOCKING parity tests pass within tolerance
- Phase 7-8: registry + final suite + commit

3 bugs caught during port + fixed before parity:
1. Cost override leak ($0.30 → $0.65 routing)
2. Off-by-one setup confirmation (strict-after)
3. Horizon doubling in parity test

### Part E — Fresh-eyes audit (on-record)
- User demanded fresh audit, no prior tool reuse
- 3 reviewers parallel, line-by-line review
- All "CRITICAL" findings verified FALSE
- Fresh parity verifier built from scratch → ALL PASS
- Audit record saved to `audit/AUDIT_RECORD_2026-07-01.md`

### Part F — Step 6 paper-live prep
- Built EquitySizer (Model B asymmetric monthly), 18 unit tests pass
- Wired sizer into LiveSafetyBroker + run_live
- CLI flags: `--use-equity-sizer / --start-balance / --risk-pct`
- DWX EA v2.15 → v2.16 patch (M15 export + 3 version strings)
- Deployed in MT5: server log confirms `v2.16 (M1/M3/M15/H1/D1 bars + ...)`
- M15 JSON files verified in DWX dir

### Part G — Going live
- Dry-run smoke: 20 bars processed, 0 trades, no errors
- Real paper-live launched both legs in background
- Bug #4 caught: `ignore_events_before` runner kwarg leaked into strategy factory → `TypeError`
- Fix: `**_runner_kwargs` absorption in A+D subclasses
- Both legs relaunched green on JM-Demo2
- User asked about visibility ("how will we know if signals fire?")
- Identified instrumentation gap → deferred to next session as option (d)

### Part H — End-of-session prep
- Triggered LIVE_DISABLED kill switch
- Stopped both paper-live procs
- Committed kwargs fix

## What's Live

- **Branch:** `fib-v2-clean`
- **Tip:** `1a7d82814` (kwargs fix)
- **Remote:** pushed through `6e9890859`; **kwargs fix `1a7d82814` is LOCAL ONLY, not pushed yet**
- **DB:** `golddigger_bt` has 2 paper-live runs: A `e13b655d`, D `eeec168f` — both ran briefly, no closed trades
- **DWX EA:** v2.16 deployed in MT5 on XAUUSD.ecn chart, M15 export confirmed
- **Paper-live procs:** STOPPED (LIVE_DISABLED kill switch active in repo root)
- **LIVE_DISABLED file present** — remove before next live launch

## Commits This Session

| SHA | Description |
|---|---|
| `1a7d82814` | fib_v2_intraday: absorb runner-only kwargs (LOCAL ONLY, not pushed) |
| `6e9890859` | Step 6 prep: Model B sizer + DWX EA M15 patch doc |
| `1d278cc70` | Audit record: A+D intraday final parity proof (2026-07-01) |
| `6e674f584` | Port A+D intraday Fib V2 to bt_engine (production-ready) |

## Outstanding Issues

### CRITICAL
- **`1a7d82814` not pushed** — local-only commit. Push before next session.
- **`LIVE_DISABLED` file present in repo root** — must remove (`rm LIVE_DISABLED`) before next live launch.

### HIGH
- **No per-bar scan visibility** — sub-bar gate decisions (pivot detected, setup built, zone hit, confirm candle, session, regime, min_risk, dedup) are NOT logged. Currently see `[BAR]` + `[SIGNAL]` + `[ENTRY_FILL]` + `[EXIT_*]` only. Decision deferred to next session as option (d).
- **No live-data dashboard** — bt_journal_events table populated but no UI to view in real-time.
- **`require_demo=True` is default but `--allow-non-demo` exists** — if accidentally passed on a real account, real money trades. Confirm config-locked before scaling.

### MEDIUM
- **Position cap=4 untested in live** — research said max concurrent = 10 sometimes. With 0.05 max_lot AND cap=4, worst-case exposure = $300 risk ≈ 6% of $5k. Tolerable but not stress-tested live.
- **Sizer never produced a real fill yet** — only the order-submit code path ran in dry-run. The lot calculation math is unit-tested but not end-to-end-tested against broker.
- **`research/.DS_Store` modified but uncommitted** — macOS Finder leftover. Ignore.

### LOW
- DWX EA also exports BRENT M15 bars (BRENT was in InpSymbols). Not used by intraday A+D. Harmless.

## Decisions Made

- **Risk = 1.5% Model B asymmetric monthly.** Not 0.5% (too slow), not 2% (-94% worst intra-month). Math vs psychology trade-off resolved.
- **Strategy stays pure (qty=1.0 placeholder); sizer in runner.** Keeps strategy parity-testable without equity state.
- **Going live on JustMarkets-Demo2 (NOT real money) — but same code path** so all bugs caught here are bugs caught for real.
- **Audit-record-on-record commit** — `audit/AUDIT_RECORD_2026-07-01.md` + fresh parity verifier saved so future 101st-time confrontations can re-run + cite.

## What's Next

1. **Push `1a7d82814` to origin** (`git push`).
2. **Remove `LIVE_DISABLED` kill switch** (`rm LIVE_DISABLED`).
3. **Build option (d) instrumentation** — task #135:
   - Add per-bar scan log lines: pivot count, setup count, gates pass/fail per leg
   - Emit StrategyEvents for every gate decision → bt_journal_events table
   - Build live-data dashboard reading bt_trades + bt_journal_events in real-time
   - Kill+relaunch both paper-live legs with new instrumentation active
4. **Run 24-48h paper-live with full visibility.** Watch for first real signal + first real fill.
5. **DB sanity check after first trade fires** — partial_*, fib_*, leg, regime fields all populated.
6. **Sign-off doc** `docs/FIB_V2_INTRADAY_PAPER_LIVE_CERTIFICATION.md` after 24h clean run.

## File Inventory

### New files
- `bt_engine/bt_engine/strategies/fib_v2_intraday/__init__.py`
- `bt_engine/bt_engine/strategies/fib_v2_intraday/config.py`
- `bt_engine/bt_engine/strategies/fib_v2_intraday/strategy.py`
- `bt_engine/bt_engine/runner/equity_sizer.py`
- `bt_engine/tests/unit/strategies/test_fib_v2_intraday_pivot_m15.py`
- `bt_engine/tests/unit/strategies/test_fib_v2_intraday_dedup.py`
- `bt_engine/tests/unit/runner/test_equity_sizer.py`
- `bt_engine/tests/parity/test_parity_fib_v2_intraday_a.py`
- `bt_engine/tests/parity/test_parity_fib_v2_intraday_d.py`
- `bt_engine/tests/integration/test_engine_fib_v2_intraday_smoke.py`
- `research/fib_retrace/gen_intraday_dedup_parquets.py`
- `research/fib_retrace/intraday_pilot.py`
- `research/fib_retrace/intraday_phase1_sweep.py`
- `research/fib_retrace/intraday_adversarial_battery.py`
- `audit/fresh_parity_verify.py`
- `audit/AUDIT_RECORD_2026-07-01.md`
- `docs/DWX_EA_M15_PATCH.md`
- `research/fib_retrace/intraday_dedup/intraday_a_trades.parquet`
- `research/fib_retrace/intraday_dedup/intraday_d_trades.parquet`

### Modified files
- `bt_engine/bt_engine/strategies/fib_v2/state.py` — added `consumed_entry_keys` field
- `bt_engine/bt_engine/strategies/registry.py` — registered `fib_v2_intraday_a` and `_d`
- `bt_engine/bt_engine/runner/live.py` — wired EquitySizer into LiveSafetyBroker
- `bt_engine/bt_engine/runner/cli.py` — added sizer flags
- `bt_engine/evidence/mar_apr_may_2026/dashboard/*` — polished display
- `bt_engine/scripts/build_dashboard.py` — refinements
- `/Users/subash/SUBASH/hand-of-midas/mql5/DWX_Server.mq5` — v2.15 → v2.16 (EXTERNAL REPO)

## Numbers to Remember

### Test counts
- 243 unit tests pass (was 214 + 11 intraday + 18 sizer)
- 4 parity tests pass (XAU/EUR baseline + intraday A + intraday D)
- 2 PTP paper-live pass
- 2 integration smoke pass (intraday A + D)
- 4 PTP evidence variants pass

### A+D paper-live expected ($5k JM-Demo2, 1.5% Model B)
- $/yr avg: **+$38,876**
- Lifetime (21yr): **+$816,401**
- Pos years: **21/21**
- Worst intra-month: $1,014 (April 2007, 20% of start)
- Wipe-outs: **0**

### Parity drift (bt vs research dedup)
- A: count 2.28%, net_R 0.16%, PF 0.96%
- D: count 4.20%, net_R 0.25%, PF 0.93%

### Bugs caught during 100th-time mandate (4 total)
1. Cost override leak — `COST_USD_DEFAULTS["XAUUSD.ecn"]=$0.30` vs FibV2Config $0.65
2. Off-by-one setup confirmation — bt allowed entry on confirm bar
3. Horizon doubling in parity test — 96 vs strict 48
4. `ignore_events_before` runner kwarg leak into strategy factory

### JustMarkets-Demo2 account
- Server: JustMarkets-Demo2
- Login: 1100447101
- Balance: $8,985.33 USD
- Leverage: 1:3000
- Symbol: XAUUSD.ecn (spread ~$0.09/oz visible)

## What I'd do if I had 1 more hour

1. `git push origin fib-v2-clean` to publish `1a7d82814`
2. Build the option (d) per-bar scan instrumentation NOW so paper-live can resume tonight with visibility — 30-min effort
3. Relaunch paper-live with `LIVE_DISABLED` removed, sleep, check DB tomorrow

But user explicitly said "do it in new session" so saving for next time.
