# Session Handoff — 2026-07-03

## Theme
Productized the dashboard (glass theme + real shadcn/ui + landing page + collapsible shell) AND fixed a real live-execution class of bugs (DWX slow-ack crash that orphaned a position), surfaced via the new cockpit.

## What Happened (Chronological)
- **UI reskin gauntlet:** neon → **black/grey glassmorphism** (design-system skill). Installed real shadcn/ui (theme-safe: `@/` alias, `cn`, glass-styled primitives, cssVariables:false so glass tokens survive). New **landing page** at `/`. **Collapsible icon-rail sidebar** + mobile bottom nav. Editorial template (numbered `SectionHeader` + `StatTile`) across pages. Fonts → Space Grotesk / Plus Jakarta / JetBrains Mono.
- **Jargon purge:** `lib/labels.ts` — `intraday_a_long`→Long, gate enums→plain English, `regime any` hidden. Applied everywhere.
- **Live cockpit:** 8 KPI tiles; unified Long/Short position cards (running $, booked, size, real $ risk, reward color-coded, R:R, live hold timer); per-digit price roll + ▲▼; odometer number animation; footer session clocks (Asia/London/NY/India).
- **Data-correctness bugs found + fixed** (all the "why is this number wrong" questions):
  - `$ PnL` used a 1.0-lot fantasy (net_r×risk×100) → showed −$4,784 vs real −$155. Now **broker_net_usd truth** everywhere.
  - Trades/Journal/Signals were scoped to ONE run → longs invisible, funnel reset on restart. Now **merge across live-strategy runs**.
  - Track Record summed the fantasy + counted retired `sdr001` → −$1,033 vs real −$221.94. Now active-strategy only, broker-$ only, wins/losses by $.
  - All trades showed same hold-time (adoption stamped entry=now). Fixed: parse broker `open_time` (UTC+3).
- **THE live bug (freeze lifted):** D-leg crashed at 22:00 on an OPEN whose EA ack timed out (>5s) but which **filled anyway** → orphaned a 0.13 short (ticket 2123464608), unmanaged, DB never recorded. Root: `submit_order` TimeoutError propagated + crashed the leg. Fixed: slow-ack recovery (tag→open_orders), engine crash-guard, per-leg adoption (no A/D double-manage), real ticket capture (inject open_orders dict key), `upsert_open` re-points re-adopted rows to current run, per-leg `max_hold_bars` (forward-only, no retroactive force-close). Restarted both legs on fixed code; all 3 positions adopted correctly.

## What's Live
- **Branch:** `fib-v2-clean` · **HEAD:** `0aafb6580` (all pushed to origin).
- **Live procs:** A (long) + D (short) on JM-Demo2, fixed code. 3 open positions: 2 long (0.01, 0.05) + 1 short (0.13), all adopted + tracked.
- **Dashboard:** uvicorn :8001 serving `dashboard/dist`.
- **Strategy/engine:** still FROZEN except the live-runner crash fixes (user lifted freeze for those).

## Commits This Session
| SHA | Description |
|---|---|
| 694411282 | Trades P&L broker-truth + partial col + live float + slow-ack recovery (partial-TP) |
| 87b68f41e | Partial-TP split booked vs floating |
| c5ddeeccc | Reconciler staleness guards |
| d4b384f9b | Dashboard productization: glass + shadcn + cockpit + live-runner crash fixes |
| 0aafb6580 | Live: $-based win/loss, per-leg hold cap on adopt, clean footer, per-digit price |

## Outstanding Issues
- **MEDIUM — 3 open positions past hold cap:** the A-longs are grandfathered (cap forward-only) so they won't auto-exit; managed by broker SL/TP only. Acceptable, but they'll sit until SL/TP or manual close.
- **LOW — open-trade fib/qty DB persistence gap** (adopted trades reconstruct from broker, not original setup).
- **LOW — Track Record on Live page** is redundant with Trades (user wants it removed next session).

## Decisions Made
- **Glass theme, real shadcn** (not look-alike) per user.
- **Hold-cap forward-only on adopt** — never retroactively market-close an existing profitable position on restart (user chose "exempt current").
- **Win/loss by $ not R** — a +R/−$ SL_BE is a $ loss.
- **Footer = sessions + health only** — account figures live in cockpit.

## What's Next (user hints)
1. **Remove Track Record section from Live page** (visible on Trades already).
2. **Deploy to Windows Contabo VPS** — the whole setup (MT5 + DWX EA + Postgres + bt_engine live runners + dashboard). Currently all on the Mac under Wine-MT5.

## File Inventory (important)
- NEW: `dashboard/src/lib/labels.ts`, `lib/utils.ts`, `components/ui/*` (shadcn + StatTile/Section/AnimatedNumber/PriceValue), `components/SessionClocks.tsx`, `MobileNav.tsx`, `pages/LandingPage.tsx`, `components.json`, `.claude/skills/design-system/`.
- MODIFIED (runner): `bt_engine/runner/live.py` (slow-ack recovery, adoption, hold cap), `core/engine.py` (submit crash-guard), `db/repo.py` (upsert re-point), `execution/dwx_broker.py` (positions ticket key), `runner/broker_reconciler.py` (staleness guards).
- MODIFIED (dashboard): tailwind.config.ts, globals.css, App.tsx, TopBar/Sidebar/StatusBar/Pane/Pill/KPI/DataGrid/Funnel/PositionCard/TradeStory/AnnotatedBarWalk, all pages.
- backend: `dashboard_backend/app/routers/runs.py` (+symbol), `app/ws/price_stream.py` (positions_live + booked).

## Numbers to Remember
- 3 open: 2118599832 (long 0.01 @4068.31), 2121027691 (long 0.05 @4064.77), 2123464608 (short 0.13 @4123.79).
- Track Record (fib legs, closed): 3 trades, −$221.94, 1 win / 2 loss.
- Orphan incident ticket: 2123464608 (the crash bug).
- Parity: A+D 21yr 0-delta still green. ~146 runner/core + 20 live-helper tests pass.
