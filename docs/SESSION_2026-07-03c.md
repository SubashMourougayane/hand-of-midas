# Session Handoff — 2026-07-03 (c) — Post-cutover hardening + 2 live-bug root fixes

## Theme
With the VPS live, hardened every live-data surface (reconcile scopes, dedup, Day-P&L, Live-page redesign), ran a trade postmortem, and root-fixed TWO real live-execution bugs found under the "freeze until issues" rule.

## What Happened (Chronological)
1. **Dashboard number reconciliation** — user kept catching mismatches (partials not counted, page scopes ambiguous, dup rows). Traced every one to broker-truth vs scope; all reconcile to broker.
2. **Trade postmortem** (`docs/POSTMORTEM_LIVE_2026-07.md`) — 5 closed since deposit: +$381.52 realized, 1 TP win (+6.25R) / 4 SL losses, all losers were shorts vs an uptrend (A+D hedge working as designed). Verdict: execution clean; sample too small to judge edge.
3. **Live page redesign** — dropped the flat 8-tile ambiguity for a HERO band: **Equity** + **Total Return since $10k deposit** (the number the user actually wanted, = equity−10000), plus a labeled supporting row (Open now / Today / Risk / market). Deposit baseline hardcoded 10000.
4. **BUG #1 (root-fixed) — partial-TP slow-ack orphan.** Ticket 2125574587: close_partial ack timed out >5s but the deal FILLED at broker → runner's single-shot verify saw stale volume → false-fail → cascaded to false SL_BE close, orphaning the 0.06 remainder (broker open, DB closed). Fix: `_close_partial_succeeded_retry` polls open_orders with backoff before judging. +test.
5. **BUG #2 (root-fixed) — restart-strands-position.** Live entry writes random trade_id; re-adopt writes uuid5(ticket) → different row for same position → original stranded open on ended run → active-run-scoped Live page hid it ("3 in MT5, 2 in Live"). Fix: `TradeRepo.supersede_stale_open_ticket` on adopt closes other open rows for the ticket → 1 open row/ticket on current run. + `LivePage.hydrate` merges across active-strategy runs (defense-in-depth). + `RunRepo.close_stale_live_runs` on startup (fixed "8 strategies" count earlier).
6. **Debug-exec service** — user wanted always-on remote debug (Contabo has no SSH). Built `midas-debug-exec` NSSM service (token-auth PowerShell exec, own port 8799, NOT via caddy). Advised against public RCE; user accepted (demo box). Port left open per user.
7. **DB baseline** — purged pre-deposit experiments + phantoms → DB matches MT5 exactly.
8. **Git cleanup** — gitignored dumps/.agents/LIVE_DISABLED.

## What's Live
- VPS 84.247.177.145 (Win Server 2025): NSSM services midas-live-a/-d/-dashboard/-caddy/-debug-exec — all RUNNING, auto-restart, start-on-boot.
- Account JM-Demo2: ~$10,957 equity, **3 open positions** (2118599832 L 0.01, 2125574587 L 0.06, 2125844421 S 0.18), all managed + shown.
- Public dashboard: https://midas.subashtrades.in (no auth).
- Branch `fib-v2-clean`, origin HEAD **4812008a8**.
- **VPS git HEAD lags at d84fd41** — running code is correct (pushed via debug-exec base64 since GitHub fetch from VPS is flaky). Re-sync pointer with `git reset --hard origin/fib-v2-clean` when connectivity recovers.

## Commits This Session (newest first, key ones)
| SHA | Description |
|---|---|
| 4812008a8 | gitignore local cruft |
| 42c1ff5cb | Runner: supersede stale open rows on adopt (restart-strand root fix) |
| 94b76c78e | Live page: hydrate across all active-strategy runs + dedup |
| bb6bb6a50 | Live partial-TP: retry broker-volume verify on slow-ack (orphan root fix) |
| f24b45e61 | Live Day P&L = realized-today + open-booked + float |
| 89e23e702 | Live page redesign: Equity + Total Return hero |
| 602f951fa | Live trade postmortem doc |
| (earlier) | mobile scroll, BT cache, ticket column, dedup, purge, backfills |

## Outstanding Issues
- **LOW — VPS git HEAD lags** (d84fd41 vs origin). Code correct; pointer stale. Reset when GitHub reachable from VPS.
- **LOW — GitHub fetch from VPS is slow/timing-out.** Deploys done via debug-exec base64 file-push. Investigate Contabo↔GitHub throughput sometime, or shallow-clone fresh.
- **WATCH (Mon) — the 2 root fixes on live trades.** Next real partial-TP + next leg-restart should confirm no orphan/strand. First live proof pending.
- **LOW — deposit baseline hardcoded 10000** in LivePage. Update if funds change, or read from broker balance history later.
- **SECURITY (user-accepted) — debug-exec port 8799 open + token in transcript.** User chose always-on. Rotate token via `install_debug_exec.ps1` if desired.

## Decisions Made
- **Live page: Total Return since deposit** is the headline number (was missing entirely).
- **Debug-exec always-on** (user override of my security objection; demo box, closed env).
- **Both bugs = valid un-freeze** ("freeze until we find issues" → found 2, fixed at root + tested).
- **Deploy via base64 file-push** when GitHub fetch times out — running code can lead the VPS git pointer.

## What's Next
1. Monday: watch first live partial-TP + leg-restart for clean recovery (the 2 fixes).
2. Re-sync VPS git pointer when GitHub reachable.
3. Optional: re-run postmortem at 30+ closed trades (edge, not just execution).
4. Standby over the weekend (Fri night IST / US July-4 holiday — thin tape).

## File Inventory (important)
- NEW: `scripts/windows/debug_exec_server.py`, `install_debug_exec.ps1`; `docs/POSTMORTEM_LIVE_2026-07.md`; `docs/SESSION_2026-07-03c.md`.
- MODIFIED (runner/db — un-freeze): `bt_engine/runner/live.py` (partial-TP retry-verify + supersede-on-adopt), `bt_engine/db/repo.py` (`supersede_stale_open_ticket`, `close_stale_live_runs`).
- MODIFIED (dashboard): `pages/LivePage.tsx` (hero redesign, day-pnl, hydrate-across-runs), `pages/TradesPage.tsx` (ticket col, dedup, filter bar, booked), `pages/SignalsPage.tsx` + `pages/JournalPage.tsx` (editorial + mobile), `components/DataGrid.tsx` (h-scroll).
- Tests: `bt_engine/tests/unit/runner/test_live_helpers.py` (+retry-verify test). 78 runner tests pass.

## Numbers to Remember
- Deposit: 2026-07-01 13:37 UTC, $10,000 baseline.
- Closed realized since deposit: +$381.52 (1 TP +$823.64, 4 SL: −60.45/−155.49/−108.42/−117.76).
- Equity ~$10,957 · Total Return ~+$957 (+9.6%) · 3 open positions.
- 78 runner unit tests pass. Debug-exec token: in `install_debug_exec.ps1` service env (rotate to invalidate).
