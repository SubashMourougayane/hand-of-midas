# Session Handoff — 2026-07-12

## Theme
The partial-TP over-count turned out to be load-bearing — it inflated the ENTIRE
certified A+D edge ~2×. Built the single live-code-path BT (live==BT by construction),
found the ONE real edge (no-partial + cost-filter, live=BT proven), and then
exhaustively buried the entire retail SMC/ICT space (250+ combos, 0 survivors). Ended
by planning an economic-signal edge hunt for next session.

## What Happened (Chronological)

1. **Over-count is worse than "1.5× inflation" — it's the whole edge.** Fresh-eyes
   re-verified: `bracket.py` books `outcome = tp_r + partial` (full runner + partial)
   when only half the position runs. The deployed unit test even *asserts* the buggy
   2.5. Three independent methods agreed to the penny: additive +7,655R / PF 1.49 vs
   **physical −2,293R / PF 0.85 (flat-risk 21yr)**. My earlier "physical +3,397" was
   ALSO wrong (mis-scaled losers). Physical + Model-B **WIPES from 2006** (cheap-gold
   era is net-negative on cost). The edge is **regime-gated** — positive only when
   gold is expensive (cost_r small).
2. **Built the single live-code-path BT** (user: "I need live == BT"). Made
   `run_live` deps-injectable (`bt_mode` + provider/clock/broker overrides); new
   `runner/sim_broker.py` (FakeBridge over the REAL DWXBrokerAdapter — physical fills
   from bars); new `scripts/run_live_path_bt.py`. Reuses the REAL LiveSafetyBroker
   (live sizing) → maximal single-source. Committed `d75ff8771`. Fixed 2 bugs:
   fill-timestamp (now→bar-ts) + speed (throttled per-bar snapshot/sweeps, 84s→47s).
3. **Reconciled the confusion** (dashboard 5,209 vs cert 7,655 vs physical −2,293):
   dashboard = old seed run `c8b0728d`; all additive numbers were illusions; only the
   live broker (+$3,124/8d real) is truth.
4. **Deliverables:** video brief (`HAND_OF_MIDAS_INTRO_VIDEO_BRIEF.md`) + landing
   review (numbers were stale seed; reverted my edit after the wipe discovery).
5. **★ FOUND THE REAL EDGE.** User's insight ("trades survive at BE, rarely TP") +
   my exit-sweep → **drop the partial-TP** (it capped the fat tail AND was the bug) +
   **cost_r ≤ 0.12 filter** (skip trades where $0.65 > 12% of stop). Full iron-clad
   battery PASSED: PF 1.20, OOS(2020+) 1.24 > IS 1.11, bootstrap P(net≤0)=0.0000,
   delay-robust, threshold-stable (monotonic), $5k→$56k 21yr NO-WIPE. Registered as
   `fib_v2_intraday_a_plus_d_edge`, **live=BT proven** (n 5949 vs 5924, PF 1.194 vs
   1.196). Committed `ad939cbea`. The FIRST thing to survive the battery.
6. **Proved the "5min TP-in-1hr" dream is a myth:** momentum + reversion both lose
   (240k trades, PF 0.34-0.47); time-of-day drift = gap artifact; ORB (basic/filtered/
   fade) PF 0.31-0.84. The edge NEEDS hours (1h-cap PF 0.79, 8h-cap 1.14, 24h 1.20).
7. **Exhaustively buried retail SMC/ICT** (Downloads/Desktop PDFs + images) to the
   dot: COBRAX, S1 (delay+1 kills it), One-Setup-for-Life (0.53), Diamant Capital
   (108 combos, fixed their shift(-2) look-ahead, 0 ≥1.0), SMC-Plan (144 combos, 0),
   CHoCH-reversal, Quant-Signals-classics. **250+ combos, 7 sources, 0 survivors.**
   Wrote `docs/EDGE_AUDIT_IRONCLAD.md` (the 8-gate battery).
8. **Planned next:** `docs/ECONOMIC_SIGNAL_EDGE_HUNT_PLAN.md` — signals with economic
   thesis (real yields/DXY, COT, cross-asset lead-lag, SMT) + OHLCV-only anomalies
   (overnight drift ★, session breakout, VWAP reversion, ATR/vol breakout). Breadth-
   first, Group B (OHLCV) first (no data fetch). **Awaiting user "start."**

## What's Live
- VPS A+D on `42ec74c` (strict 2%, additive over-count STILL in deployed live code —
  live sizer runs hot; broker balance is physical/correct). Real live P&L +$3,124/8d.
- Branch `single-live-path-bt`, tip `ad939cbea` + uncommitted physical bracket fix
  (committed as part of this handoff). NOT pushed to origin (backup gap: `d75ff8771`,
  `ad939cbea`, + the fix commit are local-only).

## Commits This Session
| SHA | Description |
|---|---|
| `d75ff8771` | single live-code-path BT — live == BT by construction |
| `ad939cbea` | audited A+D edge variant (no-partial + cost_r filter, live=BT proven) |
| (this handoff) | physical bracket over-count fix + regenerated parquets + docs |

## Outstanding Issues
- **CRITICAL:** the DEPLOYED live A+D still runs the additive over-count (bracket.py at
  `42ec74c`). The physical fix is on `single-live-path-bt` but NOT deployed. Live sizer
  over-sizes between restarts (re-hydration corrects). Decide: deploy the fix (baseline
  becomes physical) or deploy the `_edge` variant.
- **HIGH:** local commits not pushed — backup gap. `git push origin single-live-path-bt`.
- **HIGH:** the `_edge` variant is live=BT-proven at the BT level but NOT real-broker
  reconciled (paper/demo Gate 1 pending before real money).
- **MEDIUM:** re-baseline all cert docs/dashboard on physical numbers (the frozen cert
  is void — `[[fib-v2-certified-frozen]]` needs restatement).

## Decisions Made
- **Drop the partial-TP** (it was the over-count source AND capped the fat-tail edge).
- **Cost_r ≤ 0.12 filter** is the real lever (monotonic → not curve-fit). Both causal.
- **Retail SMC/ICT space is closed** — 250+ combos, 0 survivors, do not re-test.
- **Next frontier = economic signals**, not chart patterns (breadth-first, gamma
  deferred — no historical options feed).

## What's Next
1. Push the branch (backup).
2. On user "start": run Group B (OHLCV anomalies, overnight-drift ★) — fastest path to
   a possible survivor, no data needed. Then Group A (fetch yfinance/CFTC/OANDA).
3. Decide live deployment: physical baseline vs `_edge` variant → real-broker Gate 1.

## File Inventory
- New: `bt_engine/bt_engine/runner/sim_broker.py`, `bt_engine/scripts/run_live_path_bt.py`,
  `docs/EDGE_AUDIT_IRONCLAD.md`, `docs/HAND_OF_MIDAS_INTRO_VIDEO_BRIEF.md`,
  `docs/ECONOMIC_SIGNAL_EDGE_HUNT_PLAN.md`, `research/fib_retrace/exit_sweep_physical.py`,
  `research/smc_campaign/s1_clean_delay.py`.
- Modified: `runner/live.py` (bt_mode injectable + perf throttle), `core/bracket.py`
  (physical runner_frac fix), `strategies/fib_v2/{config,strategy}.py` (max_cost_r),
  `strategies/fib_v2_intraday/{config,strategy,combined}.py` + `registry.py` (edge
  variant), `safety_net_sweep.py` + intraday parquets (physical regen).

## Numbers to Remember
- Additive (ILLUSION, void): 27,959 tr, +7,655R, PF 1.49, $534k, 21/21 pos yrs.
- Physical (truth): flat-risk 21yr −2,293R / PF 0.85; Model-B WIPES from 2006.
- **Real edge (no-partial + cost_r≤0.12):** PF 1.20, netR +671, OOS 1.24, bootstrap
  p=0.0000, $5k→$56k 21yr no-wipe; 8h-intraday PF 1.14. live=BT: PF 1.194 vs 1.196.
- Live A+D real broker: **+$3,124 / 8 days** (physical, reconciled).
- Graveyard: 20+ strategies, 250+ combos, 0 survivors.
