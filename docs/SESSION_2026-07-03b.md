# Session Handoff — 2026-07-03 (b) — VPS Deployment + Live Data Hardening

## Theme
Deployed the full live stack to the Windows Contabo VPS (SSH-less, RDP-driven) and hardened every live-data surface the dashboard exposes — reconcile, dedup, KPIs, filters, page design — down to a clean track-record baseline from the 10K deposit.

## What Happened (Chronological)
1. **Dashboard optimizations** (session start): removed Track Record from Live page (killed the 50k-row/30s poll), debounced WS trade-refetch, slowed discovery polls 30→60s.
2. **VPS deploy planned** → SSH unavailable on Contabo → pivoted to **RDP + git-pull + PowerShell** runbook. Built `bootstrap.ps1`, `restore_db.ps1`, `verify.ps1`, `verify_mt5.ps1`, `go_live.ps1`, `verify_live.ps1`, `setup_caddy.ps1`, `install_services.ps1`.
3. **Provisioned VPS**: Python 3.14 / Node / PostgreSQL 15 / NSSM. Migrated DB (406MB dump, journal 170109 matched). BT==live parity PASS on Python 3.14.
4. **Hard cutover**: stopped Mac runners → quit Mac MT5 → reconciled DB to broker truth → fresh dump → VPS restore → started NSSM services → adopted the 2 open longs (short had SL-closed). Mac retired.
5. **Public dashboard**: caddy → `https://midas.subashtrades.in` (Let's Encrypt TLS), later **auth removed** per user. Fought a pre-existing `Caddy` Windows service squatting 80/443 + port-2019 admin collision.
6. **Exec-API interlude**: user asked for a temporary token-auth PowerShell RCE server so Claude could drive the VPS via `curl` (no SSH). Used it for the rest of the session. **Still running — MUST tear down.**
7. **Live-data hardening** (the bulk): fixed a cascade of dashboard-number bugs the user caught by eye — wrong reconcile reason/amount, missing partials, blank Net R/Held, duplicate rows, "8 strategies" count, missing booked-$ in totals.
8. **Clean baseline**: purged 8 pre-deposit experiment trades + 3 phantoms → DB now holds exactly the 6 real post-deposit fib_v2 positions (matches MT5 History 1:1).

## What's Live
- **VPS** (84.247.177.145, Win Server 2025): NSSM services `midas-live-a`, `midas-live-d`, `midas-dashboard`, `midas-caddy` — all RUNNING, auto-restart, start-on-boot.
- **Public**: `https://midas.subashtrades.in` (no auth, open — user's choice; read-only view of live demo account).
- **Account**: JM-Demo2, balance ~$10,440, 1 open long (2118599832, partial-TP taken).
- **Branch** `fib-v2-clean`, **HEAD `3ae5f1fa8`**, all pushed.
- **Mac**: retired. `LIVE_DISABLED` present (harmless). Runners dead.

## Commits This Session (newest first, ~30)
Key ones:
| SHA | Description |
|---|---|
| 3ae5f1fa8 | Trades dedup picks row w/ real booked $ + open booked in $ P&L KPI |
| cae289f70 | Trades dedup by broker_ticket + drop phantoms from ledger |
| 2818b29a6 | Trades merge closed across ended runs + unified filter bar; Signals/Journal editorial + mobile |
| 4faec7676 | Runner closes orphaned live runs on startup (fixes "8 strategies") |
| 0b486ffc8 | Backfill net_r + bars_held (fixes blank cols + KPIs) |
| 2d5a8da72 | Backfill known MT5 broker P&L (aged-off deals) |
| 62644f0ca | Ticket column + fix_reconcile_proper |
| a7b5019be | go_live.ps1 cutover one-shot |
| 65c510d32 | Pre-deposit purge script |
| e5fbf8403 | install_services runs python directly (Store-stub fix) |

## Outstanding Issues
- **HIGH (security) — tear down the exec server.** `scripts/windows/deploy_exec_server.py` process still running on VPS, port 8799 open. Ctrl-C it, `Remove-NetFirewallRule -DisplayName deploy-exec`, delete the file (already git-rm'd). DEPLOY_TOKEN was a guessable placeholder — the whole channel is an internet-exposed RCE until killed.
- **LOW — A+D dual-adopt writes 2 DB rows per ticket** (same broker_ticket, 2 run_ids). Frontend dedup-by-ticket collapses them, but a direct DB query double-counts. Proper fix = adoption dedup in the runner. Not urgent.
- **LOW — Python 3.14 on VPS vs 3.12 on Mac.** Imports + parity pass; unproven long-term. Watch first VPS-originated fill.
- **MEDIUM — 24-48h watch** not yet done: confirm adoption survives reboot, first fresh VPS order fills clean, no orphans.

## Decisions Made
- **RDP + exec-API instead of SSH** (Contabo has no SSH). Exec-API is temporary + must be removed.
- **Dashboard public with NO auth** — user explicitly removed basic_auth. It's a read-only demo view.
- **Track baseline = from 10K deposit (2026-07-01 13:37 UTC)** — purged everything prior.
- **Backfill aged-off broker P&L from MT5 History** — the only source once DWX buffer rolls.
- **Runner self-closes stale runs on startup** — force-kill restarts never reach graceful close.

## What's Next
1. **Tear down exec server** (security — do first).
2. **24-48h live watch** — reboot-survival, first VPS fill, orphan check.
3. Optional: runner-side adoption dedup (kill the 2-rows-per-ticket root cause).
4. Optional: change/re-add dashboard auth if public exposure reconsidered.

## File Inventory (important)
- NEW (VPS ops): `scripts/windows/{bootstrap,restore_db,verify,verify_mt5,go_live,verify_live,setup_caddy,install_services}.ps1`, `_env.ps1`, `Caddyfile`; `scripts/{reconcile_db_to_broker,fix_reconcile_proper,backfill_known_broker_pnl,backfill_netr_barsheld,backfill_partial_booked,purge_predeposit_trades}.py`; `docs/DEPLOY_CONTABO_WINDOWS.md`, `docs/DEPLOY_RUNBOOK_RDP.md`.
- MODIFIED (runner/db): `bt_engine/runner/live.py` (close stale runs on startup), `bt_engine/db/repo.py` (`close_stale_live_runs`), `bt_engine/data/dwx_bridge.py` (DWX_DIR env override, prior).
- MODIFIED (dashboard): `pages/TradesPage.tsx` (ticket col, dedup-by-ticket, unified filter, booked in KPI), `pages/SignalsPage.tsx` + `pages/JournalPage.tsx` (editorial + mobile), `App.tsx` + `LivePage.tsx` (poll optimizations).
- REMOVED: `scripts/windows/deploy_exec_server.py` (git), 8 research parquets renamed `<`→`_lt_`.

## Numbers to Remember
- Deposit: 2026-07-01 16:37 server (UTC+3) = **13:37 UTC**, ticket 1980801930.
- Post-deposit book: 6 positions — 2121027691 TP +$823.64, 2118599832 long OPEN (partial +$62.88 booked), 4 shorts SL (−60.45, −155.49, −108.42, −117.76). Net realized ≈ +$382, +$63 booked open.
- DB after purge: 8 live-trade rows = 6 tickets (2 doubled by run_id). journal 170109→ (grew w/ live).
- ~146 runner/core + 96 repo/strategy tests pass. BT==live parity PASS on Py3.14.
- Services: midas-live-a/-d/-dashboard/-caddy. Kill switch: `C:\GoldDigger\LIVE_DISABLED`.
