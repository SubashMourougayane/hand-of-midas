# Session Handoff — 2026-06-18 → 2026-06-19

## Theme

Day 1 of 30-day discipline challenge → cap-counter rework + JM-data ingest + master RCA exposing live↔BT signal divergence as drift bug #7. Live blew up −$1,517, BT on same JM data would have made +$1,115 → diagnosis: parallel-implementation drift across 4 systems.

---

## What Happened (Chronological)

### Phase A — Bible setup (early Jun 18 IST)
- Built `docs/30-day-challenge/` folder: bible, daily template, daily log, ideas parking, weekly reports, postmortems folder.
- Anchored at $10,000 post-top-up baseline.
- Frontend hotfix: `/backtest` page crashed on `is_latest=TRUE` race condition between concurrent BT clicks → guarded `data.stats` not `data` (commit `b014083`).

### Phase B — Day 1 disaster (Jun 18 morning → night)
- 4 closed Oil trades + 2 TTL_EXPIRED + 1 BE-bug scratch = **net −$1,517 / wallet $8,484** (−15.2%).
- 3 of 4 closed trades were F28-allowed-against-bias LONGs in bearish regime.
- 7 postmortems written (4 Day 1 + 3 backfilled Jun 17 for context).

### Phase C — Bug #1 phantom BE arm
- OIL-MI-1310e9cd analysis: BE journal logged `trigger_price=$77.67` but MQL5 CopyRates proved real M1 LOW for trade window was $78.00 ($0.33 phantom gap).
- Confirmed systematic via 2nd trade (OIL-MI-08b725d3 had identical $0.31 gap).
- False alarm initially raised on wrong-side SHORT BE-SL math — RETRACTED after VERIFY (code is symmetric and correct).
- Diagnostic instrumentation shipped to all 4 systems (commit `3d66acf`): added `tick_time`, `tick_age_secs`, `trigger_bid/ask/mid/spread` to BREAK_EVEN journal events. Awaiting next live BE event for root cause.

### Phase D — Cap-counter rework (override on Day 1 freeze)
- Pre-fix: 3/3 cap exhausted by 1 SL + 2 TTL_EXPIRED → blocked Oil Micro for rest of day.
- Post-fix: TTL_EXPIRED excluded from `max_trades_per_day` count via `exit_reason NOT IN ('LIMIT_TTL_EXPIRED', 'LIMIT_TTL_EXPIRED_GRACE')` in 4 schedulers + 4 scan_status + 2 stream routes.
- BT engine reworked too (Plan B): signal-gen no longer caps; execution loop caps on FILLED trades only.
- F28 wired into all 4 BT routes (M1 audit closed) — BT now reads live `BIAS_MODE`.
- 21yr validation BT (post-rework, neutral): cumulative **+$1.93M vs Path B baseline**.
- Override logged as freeze violation. 30-day challenge restart pushed to Mon Jun 22.

### Phase E — JM data ingest (Jun 18 night)
- MQL5 export script `mql5/scripts/ExportHistoryToCSV.mq5` written + ran on JM MT5 client.
- 6 native MT5 CSVs (BRENT.ecn + XAUUSD.ecn × M1/M3/H1) covering 2016-01-04 → 2026-06-18 placed in `data/raw/F28/` (gitignored).
- Coverage: 2016-2018 daily-only; **2019-05-22 onward = full intraday** (7+ years).
- Converter `scripts/convert_jm_export_to_bt_csv.py` built — MT5 tab-separated → BT bid/ask CSV format.
- OANDA-sourced symlinks deleted; JM-derived CSVs moved into `data/raw/`. **Single source of truth = JM/MT5 data.**

### Phase F — Master RCA (Jun 19 early hours)
- BT(JM, neutral) Jun 11–18 = +$1,115 across 4 systems vs live JM same window −$1,517 → **Δ $2,632 on identical broker prices**.
- Per-system RCAs spawned via 4 deep-dive subagents.
- **Per-system smoking guns identified:** Oil Macro D1 (TP-formula), Gold Macro D3 (cross-strategy lock), Gold Micro D3+D4 (rolling-window iteration + dedup keying), Oil Micro D4 (account-wide MT5 lock).
- 6 production-only gates wrapping signal-gen common across all 4 systems.
- 5 RCA reports + 1 master synthesis + 1 refactor plan written.

### Phase G — BT verification postmortem (Jun 19)
- User asked: "are BT trades genuine or phantom?"
- Walked 3 Oil Micro Jun 17 BT trades manually through real JM M3 OHLC.
- All 3 reconciled EXACTLY (entry post-F27-fill, partial-TP, BE arm, BE-SL, MAX_HOLD, blended pnl).
- **Verdict: BT is genuine. Zero phantom fills. Live converges to BT, not other way around.**

### Phase H — Refactor plan
- 8-phase plan, ~14h total, target Sat Jun 20 → Sun Jun 21 EOD before Mon Jun 22 challenge restart.
- 3 user decisions confirmed: pin Oil Macro BT TP to live formula = YES; keep Oil Micro account-wide MT5 lock = YES; keep Gold Macro cross-strategy lock = YES.
- User explored "delete live, rebuild from scratch" — rejected (would discard 11.5K lines of bug-fixed broker plumbing for 2K of buggy code).

---

## What's Live

- **Branch:** `midas-deploy`
- **Latest commit:** `cd4a07d` (Postmortem: BT trades verified GENUINE)
- **VPS state:** Bug #1 diagnostic deployed + cap rework deployed + F28-in-BT routes deployed. Live still in `BIAS_MODE=neutral` on all 4 systems.
- **Account:** $8,484 (post Day 1 disaster). 0 open positions at session end.
- **Services:** Gold Macro / Oil Macro / Gold Micro / Oil Micro all running on VPS via `start-win.bat`.
- **Frontend:** `/backtest` page hardened against null `is_latest` rows.
- **Data:** `data/raw/{BCO_USD,XAU_USD}_{D,H1,M3}.csv` are JM-sourced 2019-05-22 → 2026-06-18.

---

## Commits This Session

| SHA | Description |
|---|---|
| `b014083` | Fix /backtest crash when DB has no is_latest row |
| `246c792` | 30-day challenge framework: bible + templates + Day 1 baseline |
| `22d9b44` | Day 1 freeze test passed: F28-not-in-BT parked as P1 |
| `e28a3ff` | Postmortem: OIL-MI-ac215cc6 — Clean loss |
| `d398f0f` | Postmortem update: OIL-MI-ac215cc6 — F28 smoking gun |
| `cd32470` | Day 1 discipline log: 4 tests passed |
| `0351404` | Backfill: 4 Jun 17 postmortems |
| `c6c7bea` | Fix F28 framing: Jun 17 trades are PRE-RESET |
| `d8c6aad` | Day 1 observation: 2 limit orders pending |
| `ecf6e54` | Day 1: 2 pending → both LIMIT_TTL_EXPIRED |
| `05bc6a0` | Add missed-limit counterfactual ledger |
| `39d6597` | P2 idea: should LIMIT_TTL_EXPIRED count toward daily cap? |
| `3cd4b4d` | Cap counter rework + F28 wired to BT routes |
| `7d3ae8b` | 30-day challenge: extend start to Mon Jun 22 |
| `ea703e2` | Postmortem: OIL-MI-1310e9cd — Protected by BE |
| `7d2a6f4` | Postmortem update: BE got faked, TP hit 30min later |
| `72466b2` | Visual chart analysis confirms F29 candidate |
| `a085269` | P1 anomaly — BE trigger_price doesn't match MT5 visual |
| `48c8d0e` | MT5 Experts log resolves the mystery — F29 confirmed |
| `d857e9a` | TWO P0-candidate bugs found via MQL5 CopyRates proof |
| `3d66acf` | Bug #1 diagnostic: BE arm phantom-tick instrumentation |
| `2a75aa7` | Day 1 disaster log: 3 F28-allowed-against-bias LONGs all SL |
| `82982a3` | Add scripts/update_csv_from_oanda.py |
| `5bdfb66` | Add scripts/update_csv_from_mt5.py |
| `8a1297a` | Add MQL5 script to export JM/MT5 history to CSV |
| `6e7b4e0` | Add scripts/convert_jm_export_to_bt_csv.py |
| `fa28dd1` | RCA: Live vs BT signal divergence (Oil Macro) |
| `65ce3ad` | Master RCA + Refactor Plan |
| `cd4a07d` | Postmortem: BT trades verified GENUINE |

---

## Outstanding Issues

### CRITICAL
- **Refactor plan execution** — `docs/30-day-challenge/reports/REFACTOR_PLAN_LIVE_BT_UNIFY.md`. 8 phases, ~14h. Target Sat Jun 20 → Sun Jun 21. Phase 0 = baseline parity snapshot, Phase 1 = `broker_to_dataframe` adapter, Phase 2 = Oil Macro (most critical, includes TP-formula pin).

### HIGH
- **Bug #1 root cause** — diagnostic shipped, awaiting next live BE event for `tick_age_secs` data. Could invalidate F5 BE narrative if phantom-tick is systematic.
- **F28 multi-seed re-run** — pre-existing +$3.58M / 21yr was OANDA-data-based. Needs re-run on JM data post-refactor.
- **VPS CSVs not updated** — VPS still has OANDA CSVs. `git pull` won't fix (gitignored). Need scp or VPS-side MQL5 export + converter.

### MEDIUM
- 3 manual-close trades from June 14 still NULL pnl_usd
- `is_latest` race + API drift (frontend hardened, backend cleanup deferred)
- Double EXIT_FILLED journal events (June 15 dedup class)
- DB pnl_usd excludes overnight swap
- postmortem.py 3 bugs: auth header, R:R reads BE-adjusted SL, BE threshold display

### LOW
- F29 / F30 / F31 research candidates parked
- M14 daily_max_loss gate forward-looking

---

## Decisions Made

| Decision | Why |
|---|---|
| Hold F28 neutral on all 4 systems despite Day 1 −$1,517 | N=4 too small to override 21yr BT validation. Trust BT, gather evidence over 30 days. |
| Switch BT data source from OANDA to JM | OANDA BCO_USD ≠ JM BRENT.ecn (~$1+ structural offset). BT-vs-live parity requires same prices. |
| Cap-rework override on Day 1 (broke freeze) | LIMIT_TTL_EXPIRED counting toward cap was a real production bug. Restart 30-day challenge clean Mon Jun 22. |
| Plan B for cap rework (BT engine also reworked) | Half-fix would leave BT and live drifted. +$1.93M on 21yr validation justifies the depth. |
| Refactor not rebuild | 14h surgical refactor vs 19-day rebuild. Rebuild discards 11.5K lines of bug-fixed broker plumbing. |
| Pin Oil Macro BT TP formula to live's `asia_high − tp_buf` | Production has been on it. BT was the outlier. |
| Keep Oil Micro account-wide MT5 lock | Safety. Avoids cross-system race on shared BCO_USD. |
| Keep Gold Macro cross-strategy lock | Safety. Avoids multi-strategy entanglement. |

---

## What's Next

When work resumes (target Sat Jun 20):

1. **Phase 0** — baseline parity snapshot per system (signal lists from BT vs scheduler dry_run). Document in `docs/30-day-challenge/reports/PARITY_BASELINE.md`.
2. **Phase 1** — build `backend/scanner/broker_to_dataframe.py` adapter + 6+ unit tests.
3. **Phase 2** — Oil Macro: pin BT's TP formula to live's `asia_high − tp_buf`. Refactor `_run_alpha_sweep_core` to call `alpha_sweep.generate_signals()` directly. Production gates wrap. Add parity harness test.
4. **Phase 3** — Gold Macro refactor + verify D8 OANDA daily-bar alignment.
5. **Phase 4** — Gold Micro refactor + fix D2 config-key drift + D4 dedup keying.
6. **Phase 5** — Oil Micro: extract `generate_signals` from `engine.py` to `strategies/`, refactor scheduler. Fix D5 dedup + D9 hardcode.
7. **Phase 6** — Cross-system safety review + F28 multi-seed re-run on post-refactor BT.
8. **Phase 7-8** — Production smoke + dry-run flip.

After refactor → **Mon Jun 22 = Day 1 of 30-day challenge restart.**

---

## File Inventory

### New scripts
- `scripts/update_csv_from_oanda.py`
- `scripts/update_csv_from_mt5.py`
- `scripts/convert_jm_export_to_bt_csv.py`
- `mql5/scripts/ExportHistoryToCSV.mq5`

### New docs (in `docs/30-day-challenge/`)
- `README.md` (bible) + `DISCIPLINE_PLAN_30DAY.md` + `DAILY_TEMPLATE.md` + `DAILY_LOG.md` + `ideas/IDEAS.md` + `reports/WEEK_1_FREEZE.md`
- `reports/RCA_LIVE_VS_BT_SIGNAL_DIVERGENCE.md` (Oil Macro)
- `reports/RCA_GOLD_MACRO_DIVERGENCE.md`
- `reports/RCA_GOLD_MICRO_DIVERGENCE.md`
- `reports/RCA_OIL_MICRO_DIVERGENCE.md`
- `reports/MASTER_RCA_LIVE_BT_PARITY.md`
- `reports/REFACTOR_PLAN_LIVE_BT_UNIFY.md`
- `reports/POSTMORTEM_BT_GENUINE_TRADES.md`
- `postmortems/` (7 trade postmortems)

### Modified backend
- 4 schedulers (`backend{,-micro,-oil,-oil-micro}/scanner/scheduler.py`) — cap rework
- 4 BT engines — cap rework + Plan B
- 4 strategy files — signal-gen no longer caps
- 4 BT routes — F28 wired
- 4 scan_status routes — TTL_EXPIRED filter
- 2 stream routes (Macro Gold + Oil) — TTL_EXPIRED filter
- 4 live_engine.py files — Bug #1 diagnostic instrumentation

### Data
- `data/raw/{BCO_USD,XAU_USD}_{D,H1,M3}.csv` — JM-sourced replacing OANDA symlinks

---

## Numbers to Remember

| Metric | Value |
|---|---|
| Day 0 wallet | $10,000 (post top-up Jun 18 morning) |
| Day 1 net realized | −$1,517 |
| Day 1 wallet end | ~$8,484 (−15.16%) |
| Day 1 trades | 4 SL + 1 BE-bug scratch + 2 TTL_EXPIRED |
| Day 1 F28-allowed-against-bias | 3/3 LOST = −$1,518 |
| BT (JM neutral) Jun 17-18 4-system net | +$1,115 |
| BT-vs-Live divergence Day 1 | $2,632 |
| 21yr BT post-cap-rework cumulative gain vs Path B | +$1.93M |
| JM data coverage | 2016-2018 daily-only, 2019-05-22+ full intraday |
| BT trades manually verified genuine | 3/3 (Oil Micro Jun 17) |
| Phantom BE gap (2 trades) | $0.31 / $0.33 |
| Refactor plan effort estimate | ~14h over Sat Jun 20 + Sun Jun 21 |
| 30-day challenge restart | Mon Jun 22 → Tue Jul 21 |
| Total commits this session | 29 on `midas-deploy` |

---

## What I'd Do If I Had 1 More Hour

Run Phase 0 of refactor plan — generate baseline parity snapshot for all 4 systems (`baseline_bt_signals_<system>.json` + `baseline_live_signals_<system>.json` via dry_run). Diff per system. Quantify divergence with hard numbers BEFORE starting the refactor so we have a "before" to compare to "after". Saves Phase 0 from next session.
