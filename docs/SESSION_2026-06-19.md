# Session Handoff — 2026-06-19

## Theme

Phase 4-5-6 of `REFACTOR_PLAN_LIVE_BT_UNIFY.md` shipped end-to-end. Live↔BT signal-gen unified for both Micros, 3 strategy bugs fixed, gate alignment audited and corrected, BT broker-cost realism added, Macros disabled in live, 23 commits pushed, deploy completed via debug API + RDP recovery. Pre-shipping the 30-day challenge restart Mon Jun 22.

---

## What Happened (Chronological)

### Phase 0 — Baseline parity snapshot + insurance walks
- 3 insurance walks (1 trade per system) against real JM M3 OHLC. All 3 reconciled exactly — BT mechanics confirmed genuine.
- Phase 0 baseline: BT signal universe vs live dry_run. Pre-refactor: Gold Micro 91/84%, Oil Micro 90/90%.
- Discovered 3-strategy vs 1-strategy drift in Gold Micro BT (BT runs `mean_rev + cross_market + alpha_sweep`; live runs only `alpha_sweep`). Inflated all historical Gold Micro BT numbers.
- Decision logged: **DROP Macros from refactor scope**. Refactor only Micros.

### Phase 1 — broker_to_dataframe adapter (commit fecc34b)
- NEW `backend/scanner/broker_to_dataframe.py` — DWX/OANDA list[dict] → BT DataFrame. 16 unit tests.

### Phase 5 — Oil Micro refactor (commits 094318e, 97b5750, d624d53)
- 5.1-5.3: Extracted `generate_signals` from inline engine.py to `strategies/micro_alpha_sweep_oil.py`. Pure refactor, byte-identical.
- 5.4-5.7: Live scheduler now imports + calls `generate_signals()`. 250+ lines of inline strategy deleted from scheduler.
- Discovered + fixed BT lookahead bug #1 (consol gate). 7yr P&L drops $2.86M → $2.81M (-$48K).
- 5.8: Parity harness test `test_oil_micro_parity.py`. **Crucial bug found**: env-var capture timing — `BIAS_MODE` is captured at config-import time. Test fixture must set env BEFORE `import config`, or live boots with `BIAS_MODE=production` and bias filter activates → fewer signals than expected.

### Phase 4 — Gold Micro refactor (commit 6f2207f)
- Same pattern as Oil Micro Phase 5. Parity test passes.
- Discovered + fixed lookahead bug #2 (scan gate by `index > consol_last`). Without it, scan_bars at hour 3 could pair with consol at hours 14-17 of SAME DAY — temporally backward sweep.
- Discovered + fixed parity contract: `m3_window` must contain full expected bar count (15 bars for 0.75hr window). Without it, live's 50-bar M3 view truncates engulfing window → strategy's `for j in range(start_idx, len(m3_window))` starts at different bar in live vs BT → first-engulfing diverges. Same fix applied to both Micros.
- Gold Micro 7yr P&L: $930K → $763K (-$167K reflects lookahead bugs were Gold-heavier).

### Phase 6 — Shared production gates + BT realism + parity guards
- F1 + F2 — `production_gates.py` module + 47 unit tests. Audit confirmed live cooldown was production-correct (Issue #19 narrow trigger semantic).
- **Audit corrections**: initial table had wrong assumptions. Reality: BT and live had ALL the same gates (DD pause, risk×0.5, equity-MA, 5-min cooldown). Just code in different files. Saved 4-6 hours by NOT building "missing" features.
- #9 — Deleted dead `daily_max_loss` (4 sites + 2 config keys). Live's was dead code (only counted realized closed trades, missed unrealized). BT had alive version that was diverging. Removed both for parity.
- **#6 — Real drift found**: live computed equity-MA as MIDPOINT `(eq_20_ago + nav)/2`, BT used true 20-period MA. Different gate decisions. Fixed live to reconstruct 20 equity points from chronological pnl and average.
- #10 — Commission + swap added to BT. JustMarkets ECN: Oil $7/lot RT + $3/night long; Gold $6/lot RT + $2/night long. 7yr BT drops $4.07M → $3.04M (-$1M honest reflection of broker costs).
- F6 — Trade-list parity tests for both Micros. 7 tests. Initial fixture had `status == "SL"` filter that skipped Gold Micro entirely. Broadened to all exit reasons after user push-back ("burn rule" applied). 7/7 pass byte-clean.
- F7 — `scripts/phase6_smoke_test.py` — CLI tool, runs full pipeline (BT trades vs Live dry_run) for any 2-day window. Tested Jun 15-16, 17-18, 13-14, full 11-18. All PASS byte-parity.
- F5 — `docs/30-day-challenge/reports/PHASE6_BASELINE.md` final baseline doc.

### Macro disable (commit e38b288)
- Env-var gate added to both Macro `london_session_job` (Gold + Oil) and Gold's `daily_close_job` (cross_market + mean_rev).
- Default (unset) = DISABLED. Re-enable via `OIL_MACRO_SCAN_ENABLED=true` / `GOLD_MACRO_SCAN_ENABLED=true` BEFORE Python starts.
- Position monitor + pending order monitor + daily recon + heartbeat continue running so any open Macro positions exit cleanly.

### VPS deploy
- `git push origin midas-deploy` succeeded (cd4a07d..e38b288, 23 commits to remote).
- VPS reset attempted via `/exec/shell` debug API. **Burned**: kill loop included service hosting debug API → empty response, lost remote-control. User RDP'd in, killed `node` (frontend held log handle), `git reset --hard origin/midas-deploy`, ran `scripts\start-win.bat`.
- Verified post-restart: VPS at `e38b288`, all 4 services running, Macro env-gate firing (`macro_scan_disabled_phase6` log entries every cron tick), open Gold Micro position GD-MI-a94ebe4a tracked and being managed by position_monitor + check_alpha_sweep_breakeven.

---

## What's Live

- **Branch:** `midas-deploy`
- **Commit:** `e38b288` (pushed to remote, on VPS)
- **VPS:** all 4 Python services running (gold:5053, oil:5054, gold-micro:5055, oil-micro:5056) + frontend:3001
- **Env vars verified:** all `*_BIAS_MODE=neutral`, `LIMIT_DRY_RUN=false`, `EXECUTOR=mt5`, `*_MACRO_SCAN_ENABLED` UNSET (Macro disabled)
- **Macros:** scan disabled, position lifecycle still active
- **Open position:** GD-MI-a94ebe4a (Gold Micro LONG @ 4142.4, BE-armed at 4142.7, TP 4196.06)
- **Account NAV:** $8,790 ($8,547 balance + ~$159 unrealized at restart)
- **DWX EA:** alive on MT5, open_orders.json fresh

---

## Commits This Session

| SHA | Description |
|---|---|
| `5ea3510` | Phase 0: live↔BT signal parity baseline (Micros only) |
| `fecc34b` | Phase 1: broker_to_dataframe adapter |
| `094318e` | Phase 5.1-5.3: Oil Micro extract generate_signals → strategies/ |
| `97b5750` | Phase 5.4-5.7: Oil Micro live↔BT unify + BT lookahead bug fix |
| `d624d53` | Phase 5.8: Oil Micro parity harness test |
| `6f2207f` | Phase 4: Gold Micro live↔BT unify + parity contract for engulfing window |
| `6d35d27` | Phase 6 plan: shared production gates + BT realism + trade-list parity |
| `2d90514` | Phase 6 F1: production_gates module + 29 unit tests |
| `baf0988` | Phase 6 F2: live cooldown audit + cooldown_arms_on_skip helper |
| `e0be707` | Phase 6 #9: delete dead daily_max_loss check (4 sites + 2 config keys) |
| `7acd712` | Phase 6 #9 follow-up: delete BT daily_max_loss to match live |
| `0b0a99f` | Phase 6 #10: BT commission + swap (broker realism) |
| `f56de4d` | Phase 6 #6: live equity-MA fix (true 20-period MA matches BT) |
| `ac1dbb3` | Phase 6 F6: trade-list parity tests (regression guards) |
| `d62406a` | Phase 6 F6 followup: broaden no_phantom_trades to all exit reasons |
| `448875e` | Phase 6 F7: 2-day smoke test (full pipeline parity) |
| `2ca79a5` | Phase 6 F5: post-refactor baseline document |
| `e38b288` | Disable Gold Macro + Oil Macro live signal scanning (env-var gate) |

(plus pre-Phase-0 housekeeping: `6e7b4e0`, `fa28dd1`, `65ce3ad`, `cd4a07d`, `979fe5d`)

---

## Outstanding Issues

### CRITICAL — none.

### HIGH
- **Open Gold Micro position GD-MI-a94ebe4a** — entered Jun 19 08:15 UTC, BE-armed at 4142.7, TP 4196.06. Will exit cleanly on TP or BE_SL. Watch via Telegram + journal. Position predates Phase 6 deploy so exit will run on Phase 6 code (no concern; exit logic untouched).

### MEDIUM
- **Bug #1 phantom BE-arm trigger** (memory `bug_be_phantom_trigger`) still pending diagnostic from next BE event. Diagnostic shipped Jun 18 commit `3d66acf`. Not blocking deploy.
- **Macro code retained but unused** — fully reversible until deletion. Decision deferred per `project_macros_disabled_live` memory.
- **Latent: bars_held excludes F27 limit fill bars** (task #302) — when F27 fills mid-TTL, `position_exit_time` understates by N×3min. Doesn't matter today because BT and live both have it. Monitor.
- **3 manual-close trades from June 14** still NULL pnl_usd (carried from prior session).

### LOW
- F3/F4 — `apply_gates()` wiring deferred. Behavior aligned, code-quality refactor only. Do when adding a new gate.
- Cross_market.py crashes on JM data (no `close` column). Affects Gold Macro + Gold Micro BT when running ALL 3 strategies. Worked around via `strategies=["micro_alpha_sweep"]` kwarg per all Phase 6 BT runs. Macros dropped → not blocking.

---

## Decisions Made

| Decision | Why |
|---|---|
| **Drop Macros from refactor scope** | Master RCA found Oil Macro D1 (TP-formula) was Day 1 disaster cause. Macros add gate-divergence surface. Combined Micros 7yr BT $3.04M >> Macro contribution. Operational simplicity. Per `DECISION_2026-06-19_DROP_MACROS.md`. |
| Skip F3/F4 (apply_gates wiring) | Behavior already aligned via Phase 4-6. Refactoring duplicate code into shared module adds risk for zero behavior change. Do when adding new gates. |
| Delete daily_max_loss everywhere (#9) | Live's was dead code. BT's was alive but diverging. `max_trades_per_day=3` × max_risk already caps daily loss. Cleaner to remove than fix. |
| Live equity-MA fix (#6) — true MA not midpoint | Match BT's `np.mean(equity_history[-20:])`. Cross-year boundary acceptable trade-off (BT yearly reset is framing convention). |
| BT applies commission + swap (#10) | Live broker charges these automatically. BT was overstating P&L by ~$1M / 7yr. Honest projection > inflated theoretical. |
| F6 phantom-trade test broadened from `status=="SL"` to all exit reasons | Initial test silently skipped Gold Micro entirely. Falsification injection proved broadened test catches phantom fills. Stronger assertion now. |
| Macro disable via env-gate, not delete | Reversible. Position lifecycle continues for any open Macro positions. Code retained for BT historical analysis. |
| Push to VPS without waiting for open position to close | Position lifecycle is unchanged in Phase 6. Open trade exits via DWX EA managing the broker order; Phase 6 didn't touch exit walk. Position safe. |

---

## What's Next

When work resumes:

1. **Watch open position GD-MI-a94ebe4a** — first signal of "live with new code works as expected." Telegram alert when it exits. Compare actual fill to BT projection.
2. **Telegram + journal monitoring 24h** — first NEW signal post-startup-cooldown (~10:15 UTC VPS time after restart). Verify SIGNAL fired event matches gate decisions.
3. **Phase 7 — post-deploy reconciler** (separate work — not in current refactor plan): compare actual broker fills vs BT projections for N>=30 trades, measure real physics drag. Out-of-scope until trades accumulate.
4. **30-day challenge Day 1 restart Mon Jun 22** — discipline freeze. No new code changes during 7-day Phase 0 freeze.
5. **F29/F30/F31 research** parked per `feedback-no-auto-ship` and 30-day challenge Phase 0 freeze rules.

---

## File Inventory

### NEW (this session)
- `backend/scanner/broker_to_dataframe.py` (Phase 1 adapter)
- `backend/scanner/production_gates.py` (Phase 6 module: GateState, apply_gates, broker_costs)
- `backend-oil-micro/strategies/__init__.py`
- `backend-oil-micro/strategies/micro_alpha_sweep_oil.py` (extracted from engine.py)
- `tests/test_broker_to_dataframe.py` (16 tests)
- `tests/test_production_gates.py` (47 tests)
- `tests/harness/parity/test_oil_micro_parity.py` (Phase 5.8)
- `tests/harness/parity/test_gold_micro_parity.py` (Phase 4)
- `tests/harness/parity/test_oil_micro_trade_parity.py` (Phase 6 F6)
- `tests/harness/parity/test_gold_micro_trade_parity.py` (Phase 6 F6)
- `scripts/phase0_dry_run.py`, `phase0_bt_signal_universe.py`, `phase0_diff.py`, `phase0_signal_diff.py`
- `scripts/phase6_smoke_test.py` (F7 CLI tool)
- `scripts/run_oil_macro_full.py`, `run_oil_micro_full.py`, `run_gold_micro_full.py`
- `scripts/walk_oil_macro_jun17_18.py`, `walk_oil_micro_jun11_18.py`, `walk_gold_micro_jun11_18.py`, `walk_all_systems_jun11_18.py`
- `scripts/overlap_oil_macro_micro.py` (Macro vs Micro overlap analysis)
- `docs/30-day-challenge/reports/DECISION_2026-06-19_DROP_MACROS.md`
- `docs/30-day-challenge/reports/PHASE6_SHARED_GATES.md`
- `docs/30-day-challenge/reports/PHASE6_BASELINE.md`
- (Pre-Phase-0 housekeeping) `docs/30-day-challenge/reports/MASTER_RCA_LIVE_BT_PARITY.md`, `RCA_*_DIVERGENCE.md`, `REFACTOR_PLAN_LIVE_BT_UNIFY.md`, `POSTMORTEM_BT_GENUINE_TRADES.md`

### MODIFIED
- `backend/strategies/micro_alpha_sweep.py` — Phase 4 (cfg arg + lookahead fixes + parity contract + sweep_time metadata)
- `backend-oil-micro/strategies/micro_alpha_sweep_oil.py` — Phase 5 + Phase 4-style fixes
- `backend-oil-micro/backtest/engine.py` — Phase 5 extraction + #9 daily_max_loss removed + #10 commission/swap
- `backend-micro/backtest/engine.py` — same patches as oil-micro
- `backend-oil-micro/scanner/scheduler.py` — Phase 5 refactor + #9 + Macro disable doesn't apply (Micro)
- `backend-micro/scanner/scheduler.py` — Phase 4 refactor + #9
- `backend-oil-micro/scanner/live_engine.py` — #6 equity-MA fix + #9
- `backend-micro/scanner/live_engine.py` — same
- `backend-oil-micro/config.py` — DD_PROTECTION strip + BROKER_COSTS dict
- `backend-micro/config.py` — same
- `backend-oil/scanner/scheduler.py` — Macro disable env-gate (e38b288)
- `backend/scanner/scheduler.py` — Macro disable env-gate + import os

### Uncommitted (local debug scripts, not pushed)
- `scripts/debug_*.py` (6 files — phantom-fill investigation, parity test debugging)
- `scripts/simulate_live_oil_micro.py`
- `scripts/output/*.json/jsonl/csv` (gitignored test artifacts)

---

## Numbers to Remember

| Metric | Value |
|---|---|
| **Tests in place** | 57 (47 production_gates + 3 signal-parity + 4 oil trade-parity + 3 gold trade-parity) + 1 CLI smoke test |
| **Commits this session** | 23 to midas-deploy |
| **Phase 4-5-6 lines of code added** | 8,303 insertions / 722 deletions / 57 files changed |
| **7yr Oil Micro BT (post-Phase-6, JM, neutral)** | 2,642 trades / 71% WR / PF 4.24 / **$2,225,070** / -7.5% max DD |
| **7yr Gold Micro BT** | 2,170 trades / 64% WR / PF 6.86 / **$810,734** / -6.8% max DD |
| **7yr Combined honest** | **$3,035,804** / 4,812 trades / ~$442K avg full year |
| **Pre-Phase-6 BT (inflated)** | $4.07M (commission + lookahead fixes pulled it down) |
| **Live capture projection** | 70-90% (was 30-50% pre-refactor) |
| **VPS commit pre-deploy** | `8a1297a` (Jun 18) |
| **VPS commit post-deploy** | `e38b288` (today) |
| **Open position at deploy** | GD-MI-a94ebe4a, +$159 unrealized |
| **Account NAV at deploy** | $8,790 |
| **Day 1 disaster (Jun 18 pre-refactor)** | −$1,517 |
| **Phase 6 #6 equity-MA fix impact** | Cross-year boundary divergence acceptable |
| **Phase 6 #10 commission impact (Oil)** | -$998K / 7yr (-31%) |
| **Phase 6 #10 commission impact (Gold)** | -$32K / 7yr (-4%) |
| **Smoke test windows verified PASS** | Jun 13-14, 15-16, 17-18, 11-18 (all 100% byte parity) |

---

## What I'd Do If I Had 1 More Hour

1. Build the post-deploy reconciler (Phase 7): pull live's actual closed trades from `gd_trades`, compare each to BT's projection for the same signal, measure real physics drag. ~1h to write a `scripts/reconcile_post_deploy.py` that iterates last N trades and outputs a report. Without this, "live capture 70-90%" is a projection not a measurement.
2. Add `LIMIT_DRY_RUN` and `*_BIAS_MODE` validation to `scripts/start-win.bat` startup — fail fast if env vars missing instead of silently boot with `BIAS_MODE=production`. Critical lesson from Phase 5 fixture bug applies to production startup too.
3. Document the debug-API-self-destruct rule in `docs/DEBUG_API.md` so the next deploy doesn't repeat the burn.

---

## Lessons (preserved as memory entries)

- **`feedback_tests_pass_not_ship.md`** — tests pass ≠ ship. Standing rule.
- **`feedback_bt_truth_source.md`** — when live↔BT diverge, BT is truth source by default. Standing rule.
- **`feedback_debug_api_self_destruct.md`** — NEW. Don't kill the API host mid-call.
- **`bug_bt_lookahead_strategies.md`** — NEW. Three lookahead bugs in rolling-window strategies.
- **`bug_equity_ma_midpoint_vs_true.md`** — NEW. Live's midpoint ≠ true 20-period MA.
- **`project_live_bt_unified.md`** — NEW. Phase 0-6 result + state.
- **`project_macros_disabled_live.md`** — NEW. Env-gate state + reversibility.
- **`project_phase6_broker_costs.md`** — NEW. Commission + swap math + rates.
