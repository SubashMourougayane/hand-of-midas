# Session Handoff — 2026-06-17

## Theme

Filter #27 vigilant audit marathon under VERIFY→RCA→FIX→TEST framework: 11/24 backlog items shipped, 5 new bugs found mid-framework, bias-filter net-negative discovery (Filter #28 candidate), live trading −$868 worst day validated against 21yr BT distribution.

## What Happened (Chronological)

### Phase 1 — Vigilant audit
4 parallel detective agents covering Filter #27 entry / fill+exit / EA+scheduler / observability surfaces. Found **3 CRITICAL + 6 HIGH + 10 MEDIUM + 4 OBSERVABILITY items**. Wrote master backlog `docs/FILTER_27_AUDIT_BACKLOG.md` with severity, file:line, accept criteria, plan of action.

### Phase 2 — Locked the framework
User asked for standing rule: knock out items one-by-one with VERIFY→RCA→FIX→TEST + diff review. Wrote `feedback_bug_fix_framework.md` memory. Reference shape via the audit doc.

### Phase 3 — Critical tier (3/3)
- **C1** EA retcode-aware accept (v2.10→v2.12). 6 OrderSend callsites + Python `place_*_malformed_response` enrichment. Production proof: 11 historical retcode=10016 / 10018 errors went through `success=true ticket=0` path before this fix.
- **C2** SL wrong-side defensive guard. Skeptic re-review caught audit's framing was wrong (gap was Oil Macro-only, not all backends as claimed). Shipped anyway as defense-in-depth + corrected RCA in doc.
- **C3** broker-pending-no-DB-row reconcile. New `reconcile_broker_pending_orphans()` per backend, cancel-only strategy, magic filter for safety. 0 production hits historically; pure prevention.

### Phase 4 — High tier (6/6)
- **H1** `parse_dry_run_env()` helper in limit_price.py — strip+lower, recognised TRUE/FALSE sets, warn on unknown. Smoking-gun bug: `'true '` (trailing space) was silently flipping to dry_run=False.
- **H2** Per-cmd response files (v2.12→v2.13). EA `WriteFinalResponse(content, filename)` writes BOTH `responses/<filename>` and `last_response.json` (back-compat). Python correlates by filename, not mtime. Eliminates cross-process race for 4 services on shared `last_response.json`.
- **H3** `_orphan_lookup_ttl_seconds` config fallback. Was: silent None on DB hiccup → forever-stuck row. Now: falls back to `MICRO_ALPHA_SWEEP['limit_ttl_bars'] * 180` from config + logs distinct event for each path (no_journal / no_field / db_error).
- **H4** Validate `open_price` in fill detection. 3 failure modes (missing/zero/None); skip cycle + warn on bad data; retry next 30s monitor tick. Added M13 follow-up (stuck-on-bad-data escalation).
- **H5** EA HistorySelect retry pool (v2.13→v2.14). Extracted `TryProcessLostTicket` helper, added 16-slot retry pool × 3 max age. Without this, single-tick history-not-yet-populated lost the poll-detect entry permanently.
- **H6** Through-market check. Permissive thresholds (limit > ask LONG / limit < bid SHORT) preserve between-bid-ask placements that work in production (verified against OIL-MI-08b725d3 fill).

### Phase 5 — Medium tier (8 of 13)
- **M1** compute_limit_price refuses non-positive return
- **M2** log resolved entry_mode (catches "Limit"/"LIMIT" typos)
- **M3** journal LIMIT_COMPUTE_FAILED_FELL_BACK_TO_MARKET
- **M4** EA OnTradeTransaction dedup against cancelled file
- **M5** EA Fix A overflow warn (bundled with H5)
- **M6** EA-restart blind spot doc (no code change)
- **M7** per-system LIMIT_DRY_RUN env (OIL_LIMIT_DRY_RUN / MICRO_LIMIT_DRY_RUN / OIL_MICRO_LIMIT_DRY_RUN)
- **M12** Oil Macro missing `sl_too_close_to_price` upstream check (bonus finding via fix-coverage audit)

### Phase 6 — Coverage audit
User asked "did we propagate to all systems?" → grep showed:
- C1 covers all 4 (shared EA file)
- C2/C3/H1/H6 correctly N/A on Gold Macro (no Filter #27 limit path)
- All 3 limit-shipped backends got every limit-specific fix
- **Bonus: discovered Oil Macro missing `sl_too_close_to_price`** — shipped as M12

### Phase 7 — Live trading day
- 5 Brent SHORT trades, all SL'd: OIL-MI-08b725d3 (BE-stop +$9), GD-MI-1e53d69b (limit expired silent), GD-MI-a05fcfee (limit expired silent), OIL-MI-207a8552 (−$663), GD-MI-2b152d33 (−$157), OIL-MI-ea9d591d (−$318), OIL-MI-6fbb7040 (−$307), OIL-AS-e5705d4e (−$252)
- **Day net Brent: −$868** (worst day to date)
- User asked: "is strategy broken?" → ran 21yr BT distribution check on multi-trade-same-direction-all-SL'd days → **11 such days exist in 21yr** (~0.34% / once per 13 months). Today's −$616 Oil Micro slice is 27th percentile of those 11 days.
- Yesterday's daily candle was −4.5% (Brent collapse $82.94→$79.22). Bias correctly classified as bearish. Today Brent BOUNCED back from oversold → strategy was wrong-side of momentum.

### Phase 8 — Bias-filter net-negative discovery
User asked: "what if neutral every day?" → ran 21yr BT with `daily_bias` monkey-patched to NeutralBiasDict (always returns "neutral"). Same engine code path as dashboard.
- **All 4 systems improve substantially without bias filter**
- Trade counts up 30-65%, WR slightly UP (1-2pp), P&L up 33-100%+
- 21yr WITH bias: $5.54M | NEUTRAL: $9.11M | **Δ +$3.58M**
- Wrote `docs/FILTER_28_BIAS_DISABLE_RESEARCH.md` + preserved baseline script at `scripts/research/filter_28_neutral_bias_baseline.py`
- **NOT shipped** — single-seed only, no DD stress, no yearly slice. Filter #28 sweep candidate.

### Phase 9 — Postmortems
- `docs/trades/GD-MI-a05fcfee.md` (Gold Micro limit BUY @ $4323.04, expired silent)
- `docs/trades/OIL-MI-08b725d3.md` (Oil Micro SHORT BE-stopped +$9.10 — first Filter #27 fill on Oil Micro)

## What's Live

- **VPS:** EA on v2.10 (still old). Python services running pre-audit code.
- **All audit changes uncommitted on disk** — 1,632 net new lines + 5 new docs + 1 new test file
- **Branch:** midas-deploy at `ac9464d` (last pushed = 2026-06-16 journey page)
- **Account:** Brent systems −$868 today; Gold systems untouched
- **Open trades:** none (last position SL'd before session end)

## Commits This Session

**ZERO commits** — all work is on disk in working tree, awaiting user sign-off per [[feedback-no-auto-ship]].

## Outstanding Issues

### CRITICAL
- **Audit shipped on disk, not pushed.** Per `feedback-no-auto-ship`, user must approve diffs and commit. EA recompile (v2.10→v2.14) + Python services restart required after deploy.

### HIGH
- **M8/M9/M10/M11/M13** — 5 medium-tier items still pending, 5-15 min each with framework
- **O1-O4** — observability tier (dashboard + recon UI), 1-2hr total
- **Filter #28 sweep** — bias-disable BT showed +$3.58M; SUSPICIOUS until multi-seed + yearly slice + DD stress

### MEDIUM
- **M14** forward-looking daily_max_loss gate — today's −$616 day exceeded $400 threshold by 50% because gate is reactive not predictive
- **Backfill 4 zombie P&L** values from JustMarkets web (NULL since June 10)

### LOW
- **Calibration plan** — wait for ≥30 clean live trades; today added 8 to that count
- **Bar-vs-wallclock parity investigation** — `docs/INVESTIGATION_BAR_VS_WALLCLOCK_DRIFT.md`

## Decisions Made

| Decision | Why |
|---|---|
| Bug-fix framework as standing rule | User wanted vigilant 4-stage process for every backlog item |
| C2 reframed mid-RCA | Original audit claim wrong; corrected RCA preserved in doc + still shipped fix as defense-in-depth |
| H2 backward-compat both ways | Allow ANY EA + Python upgrade order during deploy |
| H6 permissive (limit > ask, not limit > bid) | Strict would have killed the OIL-MI-08b725d3 trade that filled cleanly |
| H3 behavior change documented | Pre-H3 stuck-forever scenarios now resolve via grace fallback — INVERSION of `test_no_ttl_no_grace_resolution`'s assertion |
| C3 cancel-only (not adopt) | Race orphans have no signal context; cancel safer than adopt |
| Filter #28 = NOT SHIPPED on single-seed BT | Per filter-sweep-workflow + selective-ship: full validation needed first |
| One commit at end (not per item) | User explicit instruction at session start |

## What's Next

When resuming:
1. **Read `docs/FILTER_27_AUDIT_BACKLOG.md`** for ✅/⏸ status
2. **Pick from pending M-tier:** M8 / M9 / M10 / M11 / M13 (5-15 min each with framework)
3. **Then O-tier:** O1 / O2 / O3 / O4 (observability)
4. **Then commit + push** as one atomic patch (after user sign-off)
5. **Then VPS deploy:** git pull + F7-recompile EA + restart Python services
6. **Then live verify:** watch for `responses/<filename>.json` writes, `gd_signals` row on next limit fire (M11 verification)
7. **Filter #28 sweep — separate session** (full workflow per `project-filter-sweep-workflow`)

## File Inventory

### New (uncommitted)
- `docs/FILTER_27_AUDIT_BACKLOG.md` — 24-item master backlog with severity, accept criteria, lifecycle log
- `docs/BUG_FILTER_27_PENDING_RECONCILER_GAP.md` — broker silent-expire bug class
- `docs/FILTER_28_BIAS_DISABLE_RESEARCH.md` — bias-filter net-negative finding
- `docs/trades/GD-MI-a05fcfee.md` — postmortem
- `docs/trades/OIL-MI-08b725d3.md` — postmortem
- `scripts/research/filter_28_neutral_bias_baseline.py` — reproducible baseline script
- `tests/test_filter_27_pending_monitor.py` — 200+ structural + runtime tests

### Modified (uncommitted)
- `backend-{oil,micro,oil-micro}/scanner/live_engine.py` — C2/C3/H1/H3/H4/H6/M2/M3/M7
- `backend/execution/limit_price.py` — H1 helper + M1 negative guard
- `backend/execution/mt5_executor.py` — H2 per-cmd response wiring + C1 enrichment
- `backend/notify.py` — `limit_orphan_warn` helper
- `mql5/DWX_Server.mq5` — C1 + H2 + H5 + M4 + M5 (v2.10 → v2.14, multiple breaking shape changes)
- `tests/test_limit_price.py` — M1 tests

## Numbers to Remember

- **Tests:** 207 cumulative, all green
- **EA versions:** v2.10 (VPS) → v2.11 (Fix A) → v2.12 (C1) → v2.13 (H2) → v2.14 (H5) [in code]
- **Audit progress:** 11/24 backlog items shipped, 13 pending
- **Lines changed:** 1,632 net new across 8 files + 5 docs + 1 test file
- **Today's day net:** Brent −$868 USD
- **21yr BT WITH bias:** $5,535,694
- **21yr BT NEUTRAL bias:** $9,112,546 (Δ +$3,576,851)
- **Bad-day frequency in BT:** 11 in 3,249 days = 0.34% = once per 13 months
- **Today's percentile vs BT bad days:** 27th — 8 BT days were WORSE than today
- **Total Filter #27 lifecycle (live):** 8 placements → 4 fills → 4 expires (2 silent-expire) → 4 SLs → 1 BE-stop
- **Bugs found mid-framework:** 5 (M11, M12, M13, M14, F28). 1 shipped (M12), 4 backlogged.

## What I'd Do If I Had 1 More Hour

1. Knock out M8/M9/M10/M11 (4 items × 5-10 min = 30 min)
2. Show user one consolidated diff
3. Get sign-off + commit + push
4. EA recompile request to user (single-line VPS instruction)

(Couldn't do this in this session because user wanted to wrap due to context limit.)

## Cross-references

- Memory: `session-2026-06-17-filter-27-audit` — full session log
- Memory: `bias-filter-net-negative` — Filter #28 finding
- Memory: `next-session-tasks` — refreshed task queue
- Memory: `feedback-bug-fix-framework` — standing rule used today
- Doc: `docs/FILTER_27_AUDIT_BACKLOG.md` — canonical backlog
- Doc: `docs/FILTER_28_BIAS_DISABLE_RESEARCH.md` — bias-filter sweep candidate
