# Session Handoff — July 12, 2026 (Friday evening, 5pm IST)

> **Note:** date in the file is 2026-07-12 per user. Conversation context shows a marathon spanning June 11→12 plus this evening session before going dark for a week. Prior session handoff was `docs/SESSION_2026_06_12.md`.

## Theme
Built `feature/debug-api` branch (31 read-only HTTP endpoints + `/exec/python|script|shell`) so future sessions can debug remotely without RDP'ing into the VPS. Smoke test caught a real bug — DWX EA dual-source — fixed via symlink. RCA'd "why no trade today" entirely through the API. User logging off for one week.

## What Happened (Chronological)

### Phase A — Wrap-up of marathon (mid-day)
- Memory + handoff written for the morning's work (drift bug #6, filter sweeps, observability build).
- Pushed `feature/observability-logging` → smoke-tested on VPS → user deployed.

### Phase B — User asked for a debug API
"Generic util API so you can debug without me copying logs." Scoped + built single shared `backend_common/debug_router.py` mounted by all 4 services. 24 endpoints per service + 4 cross-service aggregates on Gold port. Pure additive — parity harness bit-identical post-build. Pushed to `feature/debug-api` (`edc8d94`).

### Phase C — DWX EA bug found via smoke test (90min debug)
After deploy, `/api/gold/debug/dwx` showed `closed_orders.json: MISSING`. Walked through hypotheses:
1. EA source missing handler — falsified (`mql5/DWX_Server.mq5:555` has it)
2. Wrong path — falsified (g_folder + path matches Python's read path)
3. **EA on VPS has stale .ex5** — confirmed.

User had recompiled in MetaEditor, but MetaEditor opens whichever `.mq5` you `File → Open`. Right-click → Modify in MT5 Navigator opens the **terminal data folder** copy:
```
C:\Users\Administrator\AppData\Roaming\MetaQuotes\Terminal\E7DB6AF1FE93F292652A5D3B98342601\MQL5\Experts\DWX_Server.mq5
```
That copy was stale (predated June 10's `141772e Fix phantom fill bug` commit). F7 compiled stale source → fresh-looking but wrong `.ex5`. Verified via `findstr OnTradeTransaction` against terminal-folder copy.

**Fix:** `Copy-Item` repo source to terminal location → F7 → reload EA → smoke test (`scripts/smoke_test_dwx_close.py` placed + closed magic=200000 trade) → `closed_orders.json` appeared with the trade ticket. 🎯 OnTradeTransaction confirmed LIVE.

**Permanent fix:** symlinked terminal copy → repo copy. `git pull` now updates both. Verified `LinkType=SymbolicLink, Target=C:\hand-of-midas\mql5\DWX_Server.mq5`.

### Phase D — User asked for /exec endpoint
"Why can't we have a Python exec endpoint?" Right call — `/sql` already does worse, `/env` leaks all secrets, `/code` reads any file. Read-only-by-discipline pretense was performative. Added:
- `POST/GET /exec/python` — inline Python in-process (assign `_result` to return)
- `POST/GET /exec/script` — run a script from repo by relative path
- `POST/GET /exec/shell` — shell command, cwd=repo_root

Bumped OpenAPI to 1.1.0 (31 paths). Parity harness re-verified bit-identical.

### Phase E — RCA'd "no trade today"
User noticed M3 fakeouts but no live trades. Walked through fully via API (no RDP needed):

**Gold Macro state:**
- Asia [4170.33, 4230.45] (range 60.12)
- Today's H1 highs: 4226.07 / 4228.10 / 4225.62 / 4212.05 — max fell **2.35 short** of bearish-sweep trigger 4232.45
- Lows: 4191.99 / 4217.53 / 4202.89 / 4197.62 — never near bullish-sweep trigger 4168.33
- → Strategy correctly skipped. Wick-only M3 fakeouts don't qualify; H1 close-back-inside required.

**Bias check (yesterday's daily):**
- Gold: V1=bullish + V2=bullish (close at 96% of range) → today only **bullish sweeps** qualify
- Oil:  V1=bearish + V2=bearish (close at 11% of range) → today only **bearish sweeps** qualify

**Per service:**
- Gold Macro: bias bullish, no H1 sweep at all
- Oil Macro: 6 bias_blocks (`bias=bearish side=bullish` — bullish sweeps fired, bias killed them)
- Gold Micro: 1 bias_block (`bias=bullish side=bearish`)
- Oil Micro: maintenance gap blocked 00-02 UTC attempts

System working exactly as designed. Quiet-day baseline.

## What's Live (after session end)

- **VPS:** Contabo Windows, 4 services on 5053/5054/5055/5056, schedulers running
- **Account:** $9,276.84 balance, 0 open positions, 0 orphans
- **DWX:** OnTradeTransaction confirmed live, symlinked source → repo
- **Branches:**
  - `midas-deploy` head `4e5b666` — work from earlier today (drift bug #6, Filter #11, manual close scripts)
  - `feature/observability-logging` head `5ffbb7a` — deployed on VPS
  - `feature/debug-api` head `913e323` — deployed on VPS, **on origin**
  - **NOT merged into midas-deploy yet** — user manually deploys feature branches; merge to midas-deploy is git hygiene for next session

## Commits This Session (evening)

| SHA | Description |
|-----|-------------|
| `edc8d94` | Debug API: 24 read-only endpoints per service for remote debugging |
| `5167002` | Add DWX OnTradeTransaction smoke test script |
| `50f8fd2` | Diagnostic: print today's H1 bars + sweep eligibility |
| `913e323` | Debug API: add /exec/python, /exec/script, /exec/shell |

(Earlier today's commits are listed in `docs/SESSION_2026_06_12.md`.)

## Outstanding Issues

### MEDIUM
- **Merge `feature/observability-logging` + `feature/debug-api` into `midas-deploy`** — both already deployed live, just git hygiene. Order: observability first, then debug-api (debug-api is stacked on observability).
- **Gold Micro `market_close` fix** — VARIANT-B finding from morning, worth +$27k on 21-yr backtest. Deferred pending JM hours verification. See `bug_gold_micro_wrong_market_close.md`.
- **Over-sizing diagnostic** (commit `6c434c6`) awaits next Gold Micro live trade.

### LOW
- 12 untested EDGE_FILTERS remain. No urgency.

## Decisions Made

1. **Drop the read-only-by-discipline pretense.** `/sql` could DROP, `/env` leaks secrets, `/code` reads anything. Adding `/exec` is symmetric. The right answer is "same risk class as SSH" not "read-only."
2. **Symlink terminal copy → repo source.** Permanent fix for the dual-source class of bug. Better than documenting "remember to copy first."
3. **Don't merge into midas-deploy.** User does manual deploys; merging adds noise. Will batch on resume.

## What's Next (Friday 19 July 2026 onwards)

When user returns:

1. **First call:** `curl https://midas.subashtrades.in/api/debug/all-state` — multi-service snapshot
2. **Errors triage:** `curl 'https://midas.subashtrades.in/api/gold/debug/logs?level=ERROR&since=7d&lines=200'` for each of 4 services
3. **Trade history:** `/api/{svc}/debug/trades?status=closed&since=7d` to see what fired
4. **Phantom-fill check:** `/api/{svc}/debug/dwx | jq '.files."closed_orders.json"'` should now have entries (not "MISSING") if any trades closed during the week
5. **Reconciler health:** `/api/debug/all-orphans` to confirm no broker/DB drift accumulated
6. Then resume the deferred items above.

## File Inventory

### New
- `backend_common/debug_router.py` (~600 LOC) — shared router
- `backend_common/__init__.py`
- `docs/DEBUG_API.md` — curl examples
- `docs/DEBUG_API.openapi.yaml` — generated OpenAPI 1.1.0 spec (31 paths)
- `scripts/smoke_test_dwx_close.py` — magic=200000 close test
- `scripts/print_today_h1_bars.py` — sweep-eligibility diagnostic
- `docs/SESSION_2026_07_12.md` (this file)

### Modified
- `backend/main.py` / `backend-oil/main.py` / `backend-micro/main.py` / `backend-oil-micro/main.py` — mount debug router
- `backend_common/debug_router.py` (later) — added /exec endpoints

### VPS-only changes
- Symlink: `C:\Users\Administrator\AppData\Roaming\MetaQuotes\Terminal\E7DB6AF1FE93F292652A5D3B98342601\MQL5\Experts\DWX_Server.mq5` → `C:\hand-of-midas\mql5\DWX_Server.mq5`
- Recompiled `DWX_Server.ex5` with current source (now symlinked, so future `git pull` + F7 will stay in sync)

## Numbers to Remember

- **Today's Gold Asia range:** [4170.33, 4230.45], range 60.12. Max H1 high reached 4228.10 (2.35 short of trigger).
- **Today's bias:** Gold bullish, Oil bearish.
- **DWX smoke test ticket:** 2044318330 (failed first try, OnTradeTransaction not live), then 2044355259 (succeeded after symlink + recompile).
- **Parity harness baseline (unchanged all session):** gold_micro 80.0%, oil_micro 86.1%, gold_macro 80.0%, oil_macro 75.0%, all 100% direction agreement.
- **Endpoints:** 31 paths in OpenAPI spec.
- **User dark window:** Friday 12 July 2026 5pm IST → ~Friday 19 July 2026.
