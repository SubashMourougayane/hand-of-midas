# Session Handoff — 2026-07-02 (evening)

## Theme
Certified the Fib V2 A+D strategy (causality + parity proven), exhaustively closed the loss-reduction research campaign (nothing beats baseline), FROZE strategy/engine code, and pivoted to dashboard: real-time WS price/account streaming + positions-first Live cockpit.

## What Happened (Chronological)

### BT realism engine (P0-P2) — earlier in session
Built BT-only execution realism (per-lot cost, entry/SL slip, overnight swap, engine caps, weekend-gap slip, requote reject, partial-TP-fail). All opt-in, defaults reproduce baseline. Combined A+D single-engine strategy (`fib_v2_intraday_a_plus_d`). 307 tests pass. See `docs/audit/L99_BT_VS_LIVE_PARITY_2026-07-02.md`.

### Overnight research campaign — all rejected
- 76-candidate workflow (7 families) → 0 survivors. Overnight-carry NOT tradeable (exit-time survivorship).
- 474-combo mega-sweep (single+2way+3way) → ZERO positive delta-R.
- Concurrency/overlap, daily-bias, sentiment-sizing, S/D breakout, US-data-window sizing → all REJECTED.
- **Conclusion: baseline mathematically optimal. Partial-TP+1R caps loss tail; winners/losers entangled at entry.**

### Verification milestones
- **15-point line-by-line causality audit — ALL PASS** (`docs/audit/CAUSALITY_15POINT_LINE_AUDIT_2026-07-02.md`). 2 hostile reviewer agents + manual cross-check, every point cited to file:line.
- **21-year BT=live parity — 0 delta**, 27,965 trades identical (`bt_engine/scripts/parity_21yr_bt_vs_live.py`).
- **Paper-live certification** (`docs/FIB_V2_INTRADAY_PAPER_LIVE_CERTIFICATION.md`) — PAPER certified, real-capital NOT authorized.

### CODE FREEZE + frontend pivot
Strategy/engine frozen (monitoring mode). Dashboard work:
- Journal page → trade-story timeline (fixed [object Object], TP/SL off-chart via smart y-domain).
- Live badge → system-wide (not page-scoped).
- Trades → overnight (carry) filter.
- **Live cockpit → positions-first** + P&L hero.
- **WS price/account streaming**: server tails DWX market_data.json + account_info.json → pushes `price` + `account_live` envelopes. Killed the 15s poll. Live XAU ticker flashes per tick.
- **FIXED P&L double-count**: A+D share ONE broker account; was summing both legs' identical snapshots (showed $120 vs real $50). Now takes latest single snapshot; live P&L from `account_live` (broker truth).

## What's Live
- **Branch:** `fib-v2-clean` · **HEAD:** `0338a156c` · pushed through `cf5e95b50` (cockpit commit `0338a156c` NOT yet pushed).
- **Live procs:** A (PID 88850) + D (PID 89028) on JM-Demo2, running commit `769ce8a42` strategy code (frozen; later commits are docs/dashboard only — no restart needed).
- **Dashboard backend:** uvicorn on :8001 (restarted this session with price streamer). Serves SPA from dashboard/dist.
- **Broker:** JustMarkets-Demo2 · balance ~$9,782, equity ~$10,600 (up on the day).
- **Monitoring crons: STOPPED** (user request — both health-tick crons deleted).

## Commits This Session (evening)
| SHA | Description |
|---|---|
| 3c931774f | BT realism P0-P2 + combined A+D + journal redesign + overnight filter |
| 49747159a | Overnight research: 76 candidates, 0 survivors |
| a5f2fe908 | Overnight sweep: postmortems + 6 loss-cut families rejected |
| 2a6738e09 | 15-point causality audit ALL PASS |
| c23a7029c | 21yr BT=live parity (0 delta) + certification doc |
| cf5e95b50 | US-window sizing gauntlet REJECT + CODE FREEZE |
| 0338a156c | Live cockpit WS price/account stream + positions-first (NOT pushed) |

## Outstanding Issues
### HIGH
- **Commit `0338a156c` not pushed** — push when ready.
### MEDIUM
- **Open-trade fib/qty persistence gap** — live open trades don't persist fib_L/H/qty_lots to DB, so trade-story + $ unrealR degrade for open positions (falls back to snapshot). Instrumentation, not trading.
### LOW
- Stale docstring `fib_v2/strategy.py:18-39` (references old bar.close proxy; code uses next-open correctly).
- `_infer_server_utc_offset_hours` reads once at start (safe for JM no-DST).
- Task #145: dashboard pytest suite still pending.

## Decisions Made
- **CODE FREEZE on strategy/engine** — research exhausted, baseline optimal, monitoring mode.
- **No filter/sizing ships** — 474-combo + all families rejected. Baseline as-is.
- **Account is account-wide, not per-leg** — A+D share one broker account; never sum snapshots.
- **WS-stream everything** (price + account) — no polling, real-desk feel.

## What's Next
- Push `0338a156c`.
- Frontend polish continues (user driving): whatever the Live cockpit / other pages need.
- Optional: fix open-trade fib/qty DB persistence (needs a small live-runner change → but that's engine-adjacent, currently frozen; do only if user lifts freeze for it).
- After 30d clean paper-live + broker cost reconciliation → real-capital certification conditions.

## Numbers to Remember
- Research baseline b6604240: 27,950 trades, 8,279 R, PF 1.516, 48.9% WR, 21/21 pos years.
- Realistic (all costs, $5k): 5,209 R / $461k (−37% R vs fantasy). ~$22k/yr.
- 21yr BT=live parity: 27,965 trades, 0 delta.
- Tests: 307 pass (incl 12 realism-knob + 2 engine-cap).
- Live account: ~$10.6k equity end of session (started $10k).
