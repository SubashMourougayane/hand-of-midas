# Session Handoff — 2026-07-10 (part 2)

## Theme
Retail-PDF hunt → found + validated a real new edge (COBRAX); a LIVE orphan incident root-caused
+ fixed (int32 ticket overflow); and the session's biggest win — proving **live 3% is over-betting**
and de-risking to 2%. Continuation of the same-day part-1 (strict-drop/iFVG).

## What Happened (chronological)
1. **6 more ICT PDFs swept to the dot** (Silver Bullet x2, GhostTraders FVG, Liquidity-Sweep/Run,
   COBRAX screenshot). Reversal variants = dead random walk. Run/continuation LOOKED like the first
   real edge (XAU 1.45, cross-asset) — then the causal audit found an **M15-bias look-ahead**
   (searchsorted on label vs close time); after the fix + cost it went marginal → graveyard.
2. **COBRAX screenshot reproduced** = sweep→BOS→FVG in deep OTE(0.62–0.79)+trend-align, target
   liquidity/rr. Passed the FULL hostile battery at honest causality (the bias fix was in from the
   start): both dirs, gold-bear green, +1bar, not fat-tail, 0 known_at, cross-asset, cost-surviving,
   bootstrap 0. **First non-graveyard new-leg of the whole arc.** Built persistent `research/cobrax/`.
3. **384-combo IS/OOS sweep** → revenue lever = the TARGET (nl→rr3 = +36% XAU / +38% NAS, plateau).
4. **LIVE orphan incident** (2nd occurrence): D-leg ticket 2147543035 open at broker, invisible to
   DB/dashboard/Telegram. Root cause = **INT32 ticket overflow** (ticket > 2^31 → EA serializes as
   negative `-2147424261` → engine can't match). Booked half via DWX `CLOSE_PARTIAL` (+$358, real
   ulong ticket works), user set SL→BE, remainder risk-free. Then fixed the bug: `normalize_ticket()`
   at the bridge read boundary. Then a cascade of follow-on fixes (BE-adoption, dashboard match,
   range-bar label). Deployed all + verified live; the 0.35 now shows OPEN + managed.
5. **COBRAX additivity gate** (vs Fib V2): monthly-R corr **+0.07** → independent. Combined Sharpe
   +3.25→+3.49. Real but MINOR (~10% of A+D R).
6. **Combined-$ (one account):** adding COBRAX doesn't beat A+D@3% raw; at 3% it RUINS. Surfaced that
   **A+D@3% itself nearly ruins** (min-eq $289 = 94% DD).
7. **Baseline risk-cliff measured** → 3.5% ruins historically, 3% = 99.8% max DD, zero margin.
   **De-risked live A+D 3% → 2%** (VPS launch-arg + restart).
8. **COBRAX 3rd-leg deploy plan** written + approved + saved for next session (deferred build).

## What's Live
- VPS 6 services up. A+D at **2% risk** (was 3% — de-risked this session), still strict.
- Orphan 2147543035: adopted (0.35, est risk from TP), OPEN + managed, SL=BE/TP set. +$358 half in balance.
- Branch `fib-v2-clean` @ **57a4c181f** (NOT pushed past e387fda4a... actually pushed through e387fda4a; ce5a92a49 + 57a4c181f are local — the FE fix ce5a92a49 WAS pushed + built on VPS. 57a4c181f local only).
- No-strict A+D variants deployed to repo but INACTIVE (service still runs strict).

## Commits This Session
| SHA | Description |
|---|---|
| 8994d3985 | feat(intraday): no-strict A+D via strict_after flag + _nostrict registry variants |
| ebe505ba7 | fix(dwx): recover uint MT5 tickets from EA signed-int32 wrap (orphan root cause) |
| 0a83ebede | fix(live): adopt breakeven-stopped orphan via TP-estimated risk |
| e387fda4a | fix(dashboard): normalize int32-wrapped tickets when matching open positions |
| ce5a92a49 | fix(dashboard): stop range-bar label collision at break-even |
| 57a4c181f | docs(cobrax): 3rd-leg deploy plan + validated research engine/analyses |

## Outstanding Issues
- **HIGH:** orphan cause #2 (slip-reject stale per-trade state, the 1st orphan 2146419695) — still unfixed.
- **MEDIUM:** COBRAX 3rd-leg port not started (plan approved — `docs/COBRAX_3RD_LEG_PLAN.md`). EA M5 export is a hard external prereq.
- **LOW:** no-strict 2.5% deploy moot-ish now A+D is at 2%; COBRAX real-R is estimated for the one legacy BE-orphan (cosmetic).
- Local commit 57a4c181f not pushed.

## Decisions Made
- **De-risk live 3%→2%** — 3% is at the ruin cliff (3.5% ruins, 3% 99.8% DD). Highest-value change of the session.
- **Adopt BE-orphan via TP-estimated R** (not strand) — $ exact, R approximate/flagged.
- **COBRAX deploy: same account + patch EA for M5** (vs separate account / M1-aggregation).
- Killed the Silver-Bullet family; COBRAX kept as validated minor diversifier.

## What's Next
1. **Fix orphan cause #2** (slip-reject stale state) — the last unfixed live-money bug.
2. **COBRAX 3rd-leg port** (see `docs/COBRAX_3RD_LEG_PLAN.md`): build config→htf_bias→detectors→state→strategy; parity; ownership code; ≤1% risk. EA M5 patch first for live.
3. Consider whether 2% is right or go lower (prop/FundingPips wants ≤0.5%).

## File Inventory
- New (engine/tests): `strategies/fib_v2_intraday/` flag edits, `data/dwx_bridge.py` normalize_ticket, `runner/live.py` BE-adoption, `data/broker_state.py` + `ws/price_stream.py` dashboard match, `components/RangeBar.tsx`, `scripts/prove_nostrict_a_d.py`, parity/unit tests.
- New (research/docs): `research/cobrax/*` (cobrax.py engine + additivity/combined_pnl/baseline_risk/freq/sweep), `docs/COBRAX_3RD_LEG_PLAN.md`, `docs/NOSTRICT_A_D_2026-07-10.md`.

## Numbers to Remember
- **Baseline A+D risk (real sizer, $5k):** 2%=$1.18M/min-eq$999, 3%=$2.63M/min-eq$289/99.8%DD, **3.5%=RUIN**.
- No-strict A+D 20yr: 27,965/7,655R → 30,276/8,123R (+6.1%), BT=live 0-delta.
- COBRAX rr3: additivity corr +0.07, +10% combined R; standalone ~$98k/20yr@3%; ~85 trades/yr.
- Combined A+D+COBRAX frequency: **~1,464/yr, ~122/mo, ~5.8/trading-day** (A+D is the bulk; COBRAX ~7/mo).
- Orphan ticket 2147543035 = int32 wrap of -2147424261 (=2147543035 & 0xFFFFFFFF). Booked 0.35 @4114.05 = +$358.
- Tests: normalize_ticket + adoption + dashboard-match regressions all green (52+13+30+19).
