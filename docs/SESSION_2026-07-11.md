# Session Handoff — 2026-07-11 (continuation from 2026-07-10 pt2)

## Theme
Live-money hardening (2 more orphan/partial fixes deployed), a full new-leg arc that ended
in the graveyard (COBRAX = look-ahead), no-strict proven genuinely causal, a prop-firm
feasibility study (DD-throttle is the fit path), and — biggest of all — **discovery of a
~2× P&L over-count in the certified partial-TP accounting**, plus an approved plan to fix it.

## What Happened (Chronological)
1. **Orphan cause #2 fixed + deployed** (`cc99e3aef`): stale `_last_response` on a slow-ack
   submit → bogus fill → slip-reject suppressed the real recovery → orphan. Fix: clear
   `_last_response` before send; recovered fill authoritative. +2 regression tests.
2. **Adopted-trade partial-TP loss** — 3 linked fixes (`2459af53e`/`4b6ef0624`/`760206381`):
   adoption dropped `partial_tp_at_r` (never booked +1R across restart); lookup picked the
   SUPERSEDED row; broker SL never synced to BE for adopted trades. All deployed.
3. **Exit-enforcement** (`42ec74c32`): every live walker exit now verify-closes at the
   broker (was: only TIMEOUT; SL/TP trusted to self-close → orphan when engine stop ≠ broker
   SL). Deployed. A+D restarted onto full safety build.
4. **COBRAX 3rd-leg arc** (`131590f69`→`039e2670c`): built the streaming causal port (config/
   htf_bias/detectors/state/execution/strategy), Phase-2 parity FAILED (14% match), root
   cause = research `bias_align` reads HTF bias at the OUTER loop bar (AFTER the fill, 41% of
   trades) = look-ahead. Causal bias → PF 0.82 negative. **GRAVEYARD.** Exhaustive causal
   sweep confirmed every variant dead. Kept the causal port + parity harness as evidence.
5. **No-strict re-verified REAL** (`8d2778a0f`/`2460ecea3`): decision-bar causal audit (0
   look-ahead / 30,276 trades) + BT=live 0-delta re-run (30,276 trades perfect parity). Risk-
   cliff @2%: min-eq 7.8% vs strict 20%; ruins at 3%. Verdict: keep strict@2% (no-strict
   re-leverages the cushion); drawer-ready.
6. **Prop-firm study** (`270bd8779`→`49d634d28`): GFT 2-step/Instant PRO both poor fit for
   A+D (deep-DD compounding vs hard/trailing DD). Found no-daily firms (FundedNext Stellar,
   FXIFY, The5ers) fit ~2× better. **DD-throttle overlay** = the real fix: caps max-DD to
   target, 0 blowups/20yr, ~13% return cost, edge-preserving (same signals).
7. **Fixed R:R 1:2 test** (`276c0c706`): KILLS A+D edge — pure 1:2 PF 0.72 (loser); the fat
   tail (best +36R) is load-bearing. Don't cap winners.
8. **★ Partial-TP P&L over-count DISCOVERED** (`0855e5973`/`dbc3ac41b`): from the user's
   "trades survive at BE, rarely TP" → 2nd-partial-tier test → physical recompute exposed the
   over-count. Engine books `full_lot × (tp_R + partial_bonus)` but only half runs. Real edge
   ~half certified. Verified in code + saved to memory + flagged in the reference doc.
9. **Fix plan approved** (plan file `golden-wondering-book.md`): full fix + re-baseline +
   re-cert, verify-live-first.

## What's Live
- VPS 6 services up. A+D on **`42ec74c`** (strict 2%, all 5 safety fixes incl exit-enforce),
  healthy, **flat book** (both open positions manually closed by user, engine booked
  BROKER_CLOSED cleanly, DB 0 open rows).
- NOT on VPS: everything after `42ec74c` (COBRAX, no-strict tests, prop sims, partial-TP
  finding) — all research/test-local, never deployed.
- **Live sizes HOT** (over-counts equity → sizes > 2%); re-hydration on restart corrects it.
  The partial-TP fix will make it size true 2%.

## Commits This Session (cc99e3aef → dbc3ac41b)
Live fixes: `cc99e3aef` `2459af53e` `4b6ef0624` `760206381` `42ec74c32`.
COBRAX: `131590f69` `a6bff38df` `6bb838728` `039e2670c`.
No-strict: `8d2778a0f` `2460ecea3`.
Prop/throttle: `270bd8779` `cf8732c02` `cd986c38f` `45f0544be` `49d634d28` `dc9872afc`.
Trade stats/docs: `6f665e21b` `163f43470` `276c0c706` `5fb9de32e` `0855e5973` `dbc3ac41b`.

## Outstanding Issues
- **CRITICAL:** partial-TP P&L over-count — fix plan approved, NOT started. See
  `golden-wondering-book.md` + `[[partial-tp-pnl-overcount]]`. All cert $ numbers inflated
  ~1.5×; live sizes hot until fixed.
- **HIGH:** local commits (`131590f69`→`dbc3ac41b`) not pushed to origin — backup gap.
- **LOW:** swap accrual also assumes full-position-for-life (secondary, tiny).

## Decisions Made
- COBRAX → graveyard (look-ahead, not real). No re-test.
- No-strict → keep strict@2% (real but re-leverages cushion). Drawer-ready.
- R:R 1:2 → rejected (kills the tail).
- Partial-TP fix → full re-baseline + re-cert, verify-live-first (user's call).
- DD-throttle = the prop path (base 1% + cap **8%** for real floor margin, not 10%).

## What's Next
Execute the partial-TP fix plan: `core/bracket.py` + `research/fib_retrace/safety_net_sweep.py`
runner-scaling (`(1-pct)·exit_R`), update tests, regenerate parity parquets, re-baseline all
numbers, re-cert, deploy (live then sizes true 2%). Verify against live broker $ first.

## File Inventory (new, all under research/cobrax/ unless noted)
- Live fixes: `bt_engine/runner/live.py`, `execution/dwx_broker.py`, `db/repo.py`,
  `core/bracket.py` (wick mode), + tests.
- COBRAX pkg: `bt_engine/strategies/cobrax/*` (config/htf_bias/detectors/state/execution/
  strategy) + `tests/unit/strategies/test_cobrax_detectors.py`.
- Research/sims: `cobrax.py` (bracket switch), `parity.py`, `nostrict_risk.py`, `gft_sim.py`,
  `instant_pro_sim.py`, `aligned_prop_sim.py`, `throttle_sim.py`, `prop_account_sim.py`,
  `full_stats_sim.py`, `trade_stats.py`, `rr12_sim.py`, `partial2_sim.py` + their `*.log`.
- Docs: `AD_PNL_TRADE_REFERENCE.md`, `COBRAX_3RD_LEG_PLAN.md`.

## Numbers to Remember
- A+D 21yr (CERTIFIED, additive): 27,965 trades, 13,639 wins (5,457 long / 8,182 short),
  WR 48.8%, netR +7,655, PF 1.49. Exits: SL 41% / SL_BE 35% / TIMEOUT 18% / **TP only 6%**.
- A+D 21yr (PHYSICAL, correct): **netR +3,397 / PF 1.34** — the real edge (~half).
- No-strict: 30,276 trades, +6.1%R, BT=live 0-delta, 0 look-ahead.
- COBRAX causal: PF 0.82 (dead); look-ahead headline was PF 1.57.
- DD-throttle prop (per 5K, INFLATED ~1.5×): base 1%+cap10% → median $588/mo, ~$12k/yr,
  maxDD 9.9%, 0 blowups. Real ≈ half. Use cap 8%.
- Live: broker flat, both legs pid-fresh @ 2% since 18:32 UTC 07-10 restart.
