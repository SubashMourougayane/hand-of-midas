# Session Handoff — 2026-07-10

## Theme
Deep research arc: found ONE real base-improvement (strict-after drop) and killed 2 more retail strategies — but the risk-curve reveal reframed the "upgrade" as risk-level-dependent, not a free win.

## What Happened
- **Orphan bug reconciled:** ticket 2146419695 (D-leg, 0.37 short) orphaned by slow-ack-OPEN + slip-reject stale-state → manually reconciled into DB (closed −$240.50 SL). Bug still UNFIXED.
- **Dashboard ZONE_MISS label bug** fixed + deployed (commit 40350a2fc) — decoder said "price in zone" on a miss.
- **Strict-after drop** — full audit: causally clean (0 known_at violations incl 1,745 same-bar entries), 15-pt audit, delta beats 100% random, OOS>IS, bootstrap P=0, 20yr 21/21, **BT=live 0-delta parity PASS**. +11.5% $ at 1.5%.
- **RISK-CURVE reveal (the key finding):** no-strict beats baseline at every risk 0.5–2.5% (+6.5%→+22.4%), but craters −80% at 3% (near-ruin cliff). Baseline optimal-f ≥3%, no-strict ~2.5%. At each's own optimal, baseline@3% ($2.63M) > no-strict@2.5% ($2.16M). So vs current live 3%, shipping no-strict is NOT a free upgrade.
- **Sanos iFVG** (85% WR claim) → graveyard. Mechanical PF 1.02 on NAS100. MFE=MAE (random walk). 93k-config sweep: only longs pass (NASDAQ +246% beta), not alpha. 8th kill.
- **Architecture answer:** BT + live share one strategy file + one engine (certified one-code-two-modes). Can't fork. Clean isolation = add `strict_after` config flag (default True → live unchanged) — proposed, NOT yet implemented.

## What's Live
- VPS: 6 services running. A+D legs at 3% risk. Baseline strategy (strict-after ON) unchanged — NO strategy change deployed this session.
- Only deploy this session: dashboard ZONE_MISS label fix (FE-only, no restart).
- Branch `fib-v2-clean` @ 40350a2fc. Cert-candidate doc uncommitted.

## Commits This Session
| SHA | Description |
|---|---|
| 782840f84 | docs: session handoff 2026-07-09 |
| 40350a2fc | fix(dashboard): ZONE_MISS decoder label |

## Outstanding Issues
- **HIGH:** slow-ack-OPEN + slip-reject orphan bug — real, UNFIXED (`slow-ack-open-orphan-bug`).
- **MEDIUM:** live 3% risk is aggressive — no-strict shows a near-ruin cliff at 3%. Test BASELINE at 3/3.5/4% to find its cliff → possible de-risk of current system (bigger deal than strict-drop).
- **DECISION PENDING:** strict-after drop — deploy path is the config-flag approach (research-only, live untouched). Do NOT weekend-ship at 3% (worse than baseline).
- Uncommitted: `docs/CERT_CANDIDATE_STRICT_AFTER_DROP_2026-07-10.md`, `.mcp.json`, 2 handoff docs, `research-baseline/data/` (gitignored).

## Decisions Made
- Killed the iFVG sweep (only-longs-pass = beta, confirmed). Don't grind phantom sweeps once beta signature shows.
- Did NOT ship strict-drop — risk-curve showed it's not a free upgrade at current 3%.
- Proposed config-flag architecture over raw file edit (isolates research from frozen live).

## What's Next
1. **Implement `strict_after` config flag** (default True) + 0-delta regression proving live unchanged. Safe, additive, no un-freeze.
2. **Baseline risk-cliff test** (3/3.5/4%) — is current 3% over-betting?
3. Fix the slow-ack-OPEN orphan bug.

## New Data (gitignored, local)
- `oanda_NAS100_USD_M1_2020_2026.parquet` (2.27M bars), `oanda_XAU_USD_M5_20yr.parquet`, `oanda_XAU_USD_D1_20060319_20260630.parquet`.

## Numbers to Remember
- Strict-drop: 20yr baseline 27,965/7,655R/21-21 (== cert) → no-strict 30,276/8,123R/21-21. BT=live 0-delta both legs.
- Risk curve (no-strict $, $5k): 1.5%=$855k(+11.5%), 2.5%=$2.16M(+22.4% PEAK), 3%=$525k(−80% cliff).
- Sanos iFVG: PF 1.02, WR 45% (not 85%), MFE=MAE. NASDAQ 2020-26 +246%.
- Orphan 2146419695: −$240.50 SL, reconciled.
