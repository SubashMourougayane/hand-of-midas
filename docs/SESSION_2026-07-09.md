# Session Handoff — 2026-07-09

## Theme
Ship dashboard features (manual-close, BE reconciler, 1R viz) + a heavy new-leg research sprint (6 strategies killed causally) + a live orphan-position bug found and reconciled.

## What Happened
- **Manual-close button** (engine-authoritative) built, tested (8 unit tests), deployed, **proven on a real trade** (ticket 2138423940, +$720.51 booked next bar). Full-stack: UI → auth API → DWX close → engine books DB/P&L/sizer next bar.
- **BE reconciler** (`_reconcile_partial_be`, live-only per-bar) built (9 tests), deployed — heals slow-ack partial-TP orphans.
- **1R-in-$ card stat + rangebar +1R marker** built, deployed.
- **TP fat-tail study**: confirmed the 2.618 ext (~9% hit) carries the whole edge; lowering TP goes negative below +4R. Don't touch.
- **New-leg research** (all causal, real data): ICT-zone (halves edge), 1M A+D (cost-trap, PF 1.07), EMA×ADX D1 20yr (gold beta not alpha — beaten by random-long), MTF-VWAP-engulf (PF 0.58, anti-predictive entry), time-range-sweep (PF 0.50), CHoCH (PF 0.81), S2-FVG 20yr (PF 0.45, 0/21 yrs). **All graveyard.**
- **20yr OANDA data** pulled (D1 5,615 bars + M5 1.45M bars) + stored in repo (gitignored).
- **FundingPips prop Monte-Carlo**: 0.5% risk → ~80-90% pass 2-step standard.
- **LIVE BUG**: ticket 2146419695 (D-leg, 0.37 short) orphaned by slow-ack-OPEN + slip-reject stale-state — invisible in DB/dashboard/Telegram. Closed at SL −$240.50. Manually reconciled into DB.

## What's Live
- VPS: 6 services running. A-leg pid varies, D-leg pid 10788 (both healthy, processing bars). Risk 3% both legs.
- Account JM-Demo2: balance ~$11,532, equity ~$11,636.
- Branch `fib-v2-clean` @ `a96a83a18`. Deployed to VPS (frontend rebuilt).
- TradingView MCP wired locally (`.mcp.json`, uncommitted) — Yahoo-backed, weak for gold.

## Commits This Session
| SHA | Description |
|---|---|
| 0a2d468a9 | fix(live): per-bar breakeven reconciler |
| a9e9d57a3 | feat(dashboard): manual position-close button (engine-authoritative) |
| a96a83a18 | feat(dashboard): 1R in $ + mark +1R on range bar |

## Outstanding Issues
- **HIGH**: slow-ack-OPEN + slip-reject stale-state orphan bug — real, UNFIXED. See memory `slow-ack-open-orphan-bug`. Fix belongs with slip-reject hardening: slip-reject must read fill/stop/ticket from the recovered order, not leftover locals.
- **LOW**: manual-close labels exit `BROKER_CLOSED` (generic) not a dedicated `MANUAL` reason.
- Uncommitted: `.mcp.json`, `docs/INSTAGRAM_PROJECT_HANDOFF.md`, `docs/STRATEGY_HANDOFF_FIB_V2_INTRADAY_2026-07-08.md`, `research-baseline/data/` (gitignored).

## Decisions
- Manual-close = engine-authoritative (single writer) not backend-authoritative — avoids sizer double-count/split-brain.
- New-leg hunt: use random-long benchmark as the phantom filter; confirm on full sample always (2.4yr pulses vanished on 20yr).
- Kept A+D untouched; no filter added (474-combo + this session's kills all confirm baseline optimal).

## What's Next
- Fix the slow-ack-OPEN orphan bug (highest-value real fix).
- User's next idea: quantize + sweep the **Asian-session range breakout** (pending orders above/below Asian range, 15-40 pip filter, 1:1.5 RR).
- S1 (sweep→CHoCH→OTE) remains the parked deploy-next companion to A+D.

## Numbers to Remember
- Manual-close proven: ticket 2138423940 +$720.51 (−next bar).
- Orphan loss: ticket 2146419695 −$240.50 (SL, −1R, 0.37 lot × 6.5pt).
- S2 20yr: PF 0.45, WR 20%, 0/21 pos years, −112,546R.
- EMA21/50-long 20yr: net 20.6R but random-long mean 10.3 / p95 26.8 → beats only 83% of random = beta.
- 20yr D1 = 5,615 bars; 20yr M5 = 1,446,193 bars.
