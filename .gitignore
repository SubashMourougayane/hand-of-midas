node_modules/
.next/
data/raw/*.csv
__pycache__/
*.pyc
.env
logs/

# Parity harness JSON artifacts (one per run; kept locally for trend tracking,
# not stored in git). The .gitkeep file inside parity_reports/ is intentionally
# tracked so the directory exists after clone.
tests/harness/parity_reports/*.json
scripts/output/*.log
scripts/output/*.json

# oil_micro deterministic BT outputs. Keep the directory anchor, not run data.
runs/oil_micro/bt/*
!runs/oil_micro/bt/.gitkeep

# bt_engine evidence pack: 1.4GB of regenerable CSVs (re-emit via build_fib_v2_evidence_pack.py)
bt_engine/evidence/*
bt_engine/output/

# Track the Mar-Apr-May 2026 dashboard (HTML + JS + CSS) but NOT its data/ payload
# (regenerable via scripts/build_dashboard.py).
!bt_engine/evidence/mar_apr_may_2026/
bt_engine/evidence/mar_apr_may_2026/*
!bt_engine/evidence/mar_apr_may_2026/dashboard/
bt_engine/evidence/mar_apr_may_2026/dashboard/data/

# Local scratch
try/
.coverage
.pytest_cache/

# Setuptools/build artifacts
*.egg-info/
build/
dist/

# OS junk
.DS_Store

# Large raw data (regenerable from OANDA / broker)
research-baseline/data/raw/*.csv
*.csv.gz
# All large parquet sources cached under /tmp; in-repo parquets stay tracked

# Anything >100MB silently
*.bin
dashboard/tsconfig.tsbuildinfo
dashboard/dist/
dashboard/node_modules/
scripts/windows/Caddyfile.runtime
scripts/windows/deploy_exec.log
logs/debug_exec.log
# local-only cruft (never commit)
.agents/
golddigger_bt*.dump
/LIVE_DISABLED
research/data/btc/BTCUSDT_M1.parquet
research/data/jpy/
