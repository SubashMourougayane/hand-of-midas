node_modules/
.next/
data/raw/*.csv
__pycache__/
*.pyc
.env
logs/

# Parity harness JSON artifacts (one per run; kept locally for trend tracking,
# not stored in git). The .gitkeep file inside parity_reports/ is intentionally
# tracked so the directory exists after clone.
tests/harness/parity_reports/*.json
scripts/output/*.log
scripts/output/*.json
